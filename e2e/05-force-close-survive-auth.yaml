appId: ca.naavi.app
name: 05 — force-close survives auth (V57.9.7 SecureStore catcher)
tags:
  - regression
  - critical
  - auth
---
# Reproduces the V57.9.7 fix: force-killing and reopening the app
# should NOT log the user out or show "Sign-in expired" banner.

- stopApp
- launchApp:
    clearState: false
- pressKey: Back
- assertVisible:
    text: "MyNaavi"

# Confirm session is alive at start.
- tapOn:
    label: "Message input"
- inputText: "Hello"
- waitForAnimationToEnd
- tapOn:
    label: "Send"
- extendedWaitUntil:
    visible:
      text: "Ask MyNaavi"
    timeout: 30000

# Force-stop and relaunch.
- stopApp
- launchApp:
    clearState: false
- pressKey: Back
- assertVisible:
    text: "MyNaavi"

# CRITICAL — stale-auth banner must NOT appear.
- assertNotVisible:
    text: "Sign-in expired"
- takeScreenshot: 05-after-relaunch-no-banner

# Fresh send must still work without re-login.
- tapOn:
    label: "Message input"
- inputText: "Still here"
- waitForAnimationToEnd
- tapOn:
    label: "Send"
- extendedWaitUntil:
    visible:
      text: "Ask MyNaavi"
    timeout: 30000
