# T12 — Phase 8: Merge

**Work item:** [[T12]] — Voice environment equilibrium
**Date:** 2026-08-22
**Authorised by:** Wael, 2026-08-22 — *"close phase 8"*. Per Governance §3's Phase-Gate Approval
Rule, this is his own separate word for the 7→8 transition; no earlier verdict or completed phase
carried it.
**Status:** **CLOSED.**

---

## 1. The question the item was opened to answer

> *What prevents Voice Staging from being a functional replica of Voice Production at the starting
> equilibrium?*

Wael's framing, kept at the head of every phase document: two environments only have value if you
can **start** from a state where they are equal — change staging, test, promote, return to equal. If
you cannot start from equilibrium, staging is not a rehearsal of production; it is a second system,
and *"validated on staging"* means nothing about production.

**Answered:** 26 of 32 were already equal; four obstacles, none structural.

## 2. Governance §3 Phase 8 conditions

| Condition | Status |
|---|---|
| Automated tests pass | ✅ with one stated gap — §2a |
| Manual validation passes | ✅ Phase 7 complete — 4 checks passed, 1 closed by Wael's §3 decision |
| External review completed | ✅ Phase 3 *Approved*; Phase 6 *Approved* with four PASS verdicts |
| Architectural change has updated the Architecture Reference **in this same work item** | ✅ version `2026.07.18.9` — §2b |
| No newer Architecture Reference superseded the Phase 1A version un-reevaluated | ✅ — §2b |

### 2a. Automated tests — and the one gap, stated rather than glossed

Final suite run, banner-confirmed `Testing against: STAGING (xugvnfudofuskxoknhve)`:

```
528 tests   ✓ 522 passed   ✗ 0 failed   ⨯ 0 errored   ⧗ 1 timed out   ○ 5 skipped
```

The timeout is LLM latency — a *different* prompt-regression test each run — not a broken assertion.
The 5 skips are environmental and each states its own coverage gap.

**⚠️ The gap: `tests/catalogue/t12-edge-function-parity.ts` now holds SEVEN tests, and the suite
above ran when it held six.** The seventh —
`t12.parity-check.uncovered-count-is-not-worded-as-a-backlog` — was added tonight with the parity
counter fix (`9fbdcf2`) and **has never executed inside the runner.** Its five assertions were
verified directly against the edited source, and it is a pure file-content check with no DB access,
but **that is not the same as a green suite run and this document does not claim it is.**

### 2b. Architecture Reference

Phase 6 returned **Architecture Drift Outcome 2** — divergence caused by an intentional, approved
change — which under the Drift Rule makes the Reference update a **hard precondition for merge**, not
a follow-up. Four corrections were owed and all four landed (`9845182`, amended `fee91c2`):

1. §0d's *"Nothing compares deployed Edge Function code between projects"* — false since
   `parity:verify` shipped. Replaced, and the two comparators that do **not** work are recorded with
   it: `ezbr_sha256` is not a source hash (20 flagged, 15 byte-identical), and deploy timestamps
   cannot work because code is deployed before it is committed here.
2. §0c's restatement of the same claim as the drift check's blind spot — now scoped to the 32
   voice-boundary functions, with the surviving gap named: code deployed to **both** projects and
   committed to **neither** passes both tools clean.
3. §0b's stale line reference. The review said `7224 → 7624`; **the measured line is `7637`**, the
   only `Messages.json` call in the file. `7624` is the `demoEnv` guard block.
4. §0b's *"the guard sits on every send path"* — the only one of the four that could have caused
   harm, since it asserted a safety property the document contradicted two lines later.

**Correction #4 was checked rather than reworded, and found two unguarded functions** — `send-email`
and `send-drive-file`, both sending real mail via the Gmail API with no allowlist. **Wael ruled it
closed, not a gap** (2026-08-21): staging is contained by **controlled data**, not by the guard; all
34 staging sends ever recorded went to controlled destinations. His assessment of the residual
exposure — *"where is the risk in a world that we receive hundreds of spam emails daily, and it will
be one or two emails, it does not justify"* — is recorded in §0b so it is not re-raised. No item was
opened and no code was touched.

**Version check.** Phase 1A recorded `2026.07.18.8`. The Reference now reads `2026.07.18.9`, and
**revision 9 is T12's own Phase 8 output** — it does not supersede the version this implementation
was reasoned against.

## 3. The acceptance measurement, re-run at close

Phase 6's Verification Checklist required that Phase 7 not leave the two environments in a state
where re-running the proof returned anything other than 32/32. Re-run 2026-08-22, 12:20 a.m. EST,
after every Phase 7 check:

```
identical : 32
DIFFERENT : 0
one-sided : 0
```

**Baseline: `"accepted": {}` — empty. No difference needed an excuse.**

`parity:verify` downloads the deployed source from both projects and diffs it. `parity:check`, the
pre-push tripwire, states in its own output that it is **not** proof. Only the run above is entitled
to the claim.

**Why this number is trustworthy where earlier ones were not:** `parity:verify` reproduced Phase 1's
hand measurement **exactly, by a different method**, before anything was built on it. Every prior
parity comparison in this project was confidently wrong. This is the first independently
corroborated one.

## 4. What T12 shipped

- **`scripts/deploy-edge-function.js`** — refuses to deploy a function whose source is uncommitted.
- **`scripts/edge-function-parity-check.js`** — `parity:check` (fast, manifest-based, self-declaring
  as non-proof, bound to pre-push) and `parity:verify` (downloads and diffs real deployed source).
- **Pre-push gate**, beside the drift and schema/code checks. Also asserts `OUTBOUND_ALLOWLIST` is
  unset on production — once the guard is deployed to both projects, that secret's absence is the
  only thing keeping production sends unfiltered.
- **Seven auto-tester tests** and a manifest.
- **An empty baseline.**

**Deploys, each verified individually:** `create-contact` → both. `send-sms`, `send-user-email`,
`ingest-ticket`, `send-push-notification`, `receive-demo-sms-reply` → production.

**D4 fixed a live defect:** the demo line's verbal STOP had been POSTing to a **404**, so callers
were told *"you won't hear from us again"* and no opt-out was ever written.

**The Phase 0 constraint that was superseded, and where:** Phase 0 said `send-sms`,
`send-user-email` and `send-push-notification` *"must never be promoted to production."* **Phase 1
§6.1 explicitly superseded it** on evidence — the guard is inert without `OUTBOUND_ALLOWLIST`, which
production does not have, verified live on both projects. That is why the baseline is empty rather
than holding three permanent entries.

## 5. What this work item does NOT establish

Recorded because a closure summary is exactly where scope quietly widens.

- **`ingest-ticket` and `send-user-email` were redeployed and never exercised.** Zero rows through
  either since the 4:26 p.m. deploy. Additive with the guard inert, so no change is expected —
  but expected is not observed.
- **The demo line's spoken "stop" is unproven.** Three of four links verified without a call; that
  Deepgram hears the word, and that the write lands, is not established. Wael's §3 decision.
- **Equilibrium is proven for 32 functions on the voice boundary, on one date.** Not for every Edge
  Function, and not continuously.
- **Neither parity tool can see code deployed to both projects and committed to neither.** T12 found
  two live instances of exactly that — `create-contact` on staging and
  `global-search/adapters/calendar.ts` **on production, serving real users** — both now under version
  control (`8e24aae`). The deploy wrapper prevents new ones. **Nothing has swept the remaining
  functions for existing ones**, and that sweep is [[T13]]'s, not T12's.
- **One test has never run inside the suite** — §2a.

## 6. ⭐ The honest record of where the defects came from

**Four defects surfaced during this work item. Every one was found by Wael on a phone or reading a
screen. None was found by a gate.**

| | |
|---|---|
| [[B11j]] | voice `ADD_CONTACT` sent no `user_id` — broken on **both** environments. Fixed, validated on staging, promoted. Found by T12's own T0 gate, which is the one exception |
| [[B11k]] | Naavi reports success for actions that failed. Twelve state-changing actions exposed. Caught live: `"Saved."` dispatched at `turn-exit-before-tts` while the action was still pending, then `invalid_grant` |
| [[B4z]] | confirm-then-act is policy for 21 actions and implemented for 12. **B4z was cited eight times inside the prompt and tracked nowhere in the holding list.** Restored |
| [[B11l]] | *"text me"* resolved to `AbdelMegid EL Mehelmy` — the two letters `me` matching inside "**Me**helmy" — and the card labelled him **"me"**. One tap from a real message to a real stranger |

**B4z, B11k and B11l are one weakness three ways: what Naavi tells the user about a state-changing
action is not derived from what the action will actually do.**

**And the equilibrium test itself nearly lost its answer to the same weakness.** Wael reported
*"nothing added"* and was right about what he could see — the contact had gone to the account he
called **from**, and nothing in the log or the spoken reply names the destination account. On
production both candidate accounts are named "Robert" in `user_settings.name`, so the greeting cannot
separate them either. The test was recovered by querying Google directly.

## 7. Loose ends handed on

- **[[T13]]** — five Edge Functions found live on staging and committed to nothing, now under version
  control (`63b6efb`); the promote-or-drop decision is open, one per function, and the unswept-
  functions sweep lands there.
- **Branch `t12/create-contact-user-id-resolution`** (`55f2d7e`) — merged, present locally and on
  origin, **can be deleted.** Not deleted here.
- **[[B4z]], [[B11k]], [[B11l]]** — open, none started.
- **The priority queue is headed "as of 2026-07-17"** and contains no `B11*` item, nor T5–T13, nor
  S2. It is not a current work order.

---

**T12 is closed.** The loop was shown to close: a real defect was fixed on staging, tested there,
predicted, promoted, and verified on production against a criterion committed before the promotion
existed. **One instance is one instance** — it establishes that the loop *can* close, which had never
been demonstrated in this project before.
