# ADR 0000 — Template

Copy this file to `NNNN-short-title.md` (next sequential number) for a new decision record. Per `AI_DEVELOPMENT_GOVERNANCE_v3.0_FINAL.md` §6, required for: any decision to accept duplication instead of unifying it, any decision that a capability is intentionally platform-specific rather than Shared Core, and any decision the Architecture Audit Trigger surfaces and resolves without full unification.

**Status:** Proposed / Accepted / Superseded (by ADR-NNNN)
**Date:**
**Related:** (holding-list items, Architecture Reference sections, other ADRs)

## Problem

What situation or tension prompted this decision?

## Decision

What was actually decided (or, if no deliberate decision was ever made and this ADR is documenting an unplanned state honestly, say so plainly — "no deliberate decision record exists; this reflects how the system grew" is a valid and required answer when it's the truth).

## Alternatives Considered

What other approaches were possible?

## Why Rejected

For each alternative: why it wasn't chosen. If this ADR is documenting drift rather than a real decision, state clearly which alternatives simply were never evaluated, rather than inventing a rejection reason after the fact.

## Consequences

What does this make harder? What does it enable? What ongoing cost does it carry (e.g., an Architecture Exception with a review date, per Governance §5a)?
