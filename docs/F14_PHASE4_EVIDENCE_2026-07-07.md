# F14 — Phase 4/5: Evidence Package

Per `docs/AI_DEVELOPMENT_GOVERNANCE.md` Phase 5 ("Evidence Package... If this package is missing, the task is incomplete"), filed as Phase 4 evidence per this project's established naming convention (matching `docs/F12_PHASE4_EVIDENCE_2026-07-06.md`). Implements `docs/F14_PHASE2_CHANGE_PLAN_2026-07-07.md` exactly as approved — no files touched beyond that plan's §2 list.

**Not yet pushed to production.** This package documents work completed and verified locally; commit/push is a separate, still-pending step.

---

## Summary

All 6 tasks from the approved Phase 2 plan are implemented and verified locally:
1. Build tooling added (`package.json`, `babel.config.json`, `tailwind.config.js`, `.gitignore`, `build-demos.js`) — first build process this repo has ever had.
2. `vercel.json` updated with `"framework": null` + `"buildCommand": ""`, verified against Vercel's current official documentation (Phase 2 §2 revision) to actually prevent an unintended Vercel-side auto-build once `package.json` exists.
3. Each of the 5 demos' inline JSX extracted verbatim into `src/*.jsx` — copy, not rewrite.
4. Build run (`npm install` + `npm run build:demos`): Babel CLI compiled all 5 `.jsx` files to plain JS in `dist/`, Tailwind CLI compiled `dist/demos.css`.
5. All 5 HTML files rewired: removed `babel.min.js` + `cdn.tailwindcss.com` CDN tags and the inline `<script type="text/babel">` block, added one `<link>` to `dist/demos.css` and one `<script>` to the matching `dist/*.compiled.js`. React/ReactDOM CDN tags unchanged.
6. Full verification pass per Phase 2 §6 (below).

## Files changed

| File | Status | Note |
|---|---|---|
| `package.json`, `package-lock.json` | New | Dev dependencies: `@babel/cli`, `@babel/core`, `@babel/preset-react`, `tailwindcss`. `package-lock.json` auto-generated by `npm install`, committed for reproducible installs. |
| `.gitignore` | New | `node_modules/` — confirmed not tracked (`git status` shows no `node_modules` entries). |
| `babel.config.json` | New | `{"presets": ["@babel/preset-react"]}` |
| `tailwind.config.js` | New | `content` globs point at `src/*.jsx` and all 5 HTML files. |
| `build-demos.js` | New | Build script: Babel CLI compile (5 files) + Tailwind CLI compile (1 CSS file). Run via `npm run build:demos`. |
| `src/notes-demo.jsx`, `brakes-demo.jsx`, `granddaughter-demo.jsx`, `doctor-demo.jsx`, `insurance-demo.jsx` | New | Verbatim extraction of each file's former inline JSX — mechanical move, confirmed via direct read of extracted content against original. |
| `dist/notes-demo.compiled.js`, `brakes-demo.compiled.js`, `granddaughter-demo.compiled.js`, `doctor-demo.compiled.js`, `insurance-demo.compiled.js`, `demos.css` | New (generated, committed) | Build output — never hand-edited per the rule in Phase 2 §3. |
| `notes-demo-storyboard.html`, `brakes-demo-storyboard.html`, `granddaughter-demo-storyboard.html`, `doctor-demo-storyboard.html`, `insurance-demo-storyboard.html` | Modified | 3 CDN `<script>` tags + inline JSX block removed; 1 `<link>` + 1 `<script>` added. **Verified via `git diff` on all 5 files (not inspection)** that no other line differs — the diff output for each file contains exactly the removed CDN/JSX lines and the two added lines, nothing else. "Byte-identical aside from the script/stylesheet references" is accurate as a mechanically-verified claim, not an inference. |
| `vercel.json` | Modified | Added `"framework": null` and `"buildCommand": ""` (2 lines). |

**Not touched:** any file outside `mynaavi-website`. No file outside the approved §2 list.

## Git diff (stat)

```
brakes-demo-storyboard.html        | 454 +----------------------------------
doctor-demo-storyboard.html        | 352 +--------------------------
granddaughter-demo-storyboard.html | 339 +-------------------------
insurance-demo-storyboard.html     | 319 +------------------------
notes-demo-storyboard.html         | 470 +------------------------------------
vercel.json                        |   2 +
6 files changed, 12 insertions(+), 1924 deletions(-)
```
Plus 13 new untracked files/directories (`package.json`, `package-lock.json`, `.gitignore`, `babel.config.json`, `tailwind.config.js`, `build-demos.js`, `src/` × 5, `dist/` × 6) — not yet staged or committed.

## Tests executed (this repo has no automated suite — all manual, per Phase 2 §6)

- **Visual parity, all 5 demos:** screenshots taken and compared against pre-change screenshots (same session, same scenes). Notes, Granddaughter, Doctor, Insurance confirmed pixel-identical. Brakes confirmed via console-log check (screenshot tool has a known pre-existing quirk with this file's Web Audio, unrelated to this change — see `project_naavi_demo_storyboard_visual_quality` memory).
- **Console errors:** zero, on all 5 standalone demo pages and the homepage with all 5 embedded.
- **Network requests:** confirmed zero requests to `unpkg.com/@babel/standalone` or `cdn.tailwindcss.com` anywhere — grepped the full network log.
- **Brakes Web Audio:** Play button clicked, squeal scene triggered, zero console errors after.
- **Homepage embed:** all 5 iframes render correctly together, confirmed via screenshot; zero Babel/Tailwind console warnings anywhere on the page (previously 5× each).
- **Lighthouse, before vs. after** (same methodology as Phase 1 Evidence 2 — mobile, simulated throttling, `cpuSlowdownMultiplier: 4`):

  | Metric | Before | After |
  |---|---|---|
  | Performance score | 28 | **67** |
  | Total Blocking Time | 2,820 ms | **200 ms** |
  | Script Evaluation (main-thread) | 8,732 ms | **271 ms** |
  | Time to Interactive | 11.8 s | **4.8 s** |
  | `babel.min.js` cost | 8,809 ms | **0 — no longer loaded** |

  Both runs used the same tool, same flags, same machine — a valid apples-to-apples comparison, distinct from the Phase 1 Evidence 2 run (which was against production and not directly comparable in absolute terms, per that document's own caveat).

- **Build reproducibility, actually performed (per Phase 2 §2's forward-looking note, added per external review):** backed up the built `dist/` output, then deleted both `node_modules/` and `dist/` entirely (simulating a clean checkout), then ran `npm install` followed by `npm run build:demos` from scratch. Byte-for-byte `diff` of all 6 regenerated files (`notes-demo.compiled.js`, `brakes-demo.compiled.js`, `granddaughter-demo.compiled.js`, `doctor-demo.compiled.js`, `insurance-demo.compiled.js`, `demos.css`) against the originally-built versions: **all 6 identical, zero differences.** The build is reproducible from source, not dependent on local machine state.

## Manual tests still required (not yet done)

- Re-run Lighthouse against **production** (mynaavi.com) after the push, to confirm the improvement holds outside the local dev environment — the local numbers above are a valid before/after comparison but not a production-equivalent absolute score.
- Confirm `vercel.json`'s config actually prevents an unintended Vercel-side build — check the Vercel deploy log on the first push that includes `package.json`.
- Per Phase 2 §6's real-user-responsiveness addition (external review Recommendation 1): browser memory/CPU usage and a direct "does it scroll immediately" check on the live production page, not just Lighthouse's synthetic score.

## Rollback instructions

Per Phase 2 §7: `git revert` the commit containing these changes. The 5 HTML files return to loading React/Babel/Tailwind from the CDN exactly as before; `dist/`, `src/`, `package.json`, and the config files are removed. No backend, database, or runtime configuration outside this one static site is affected — rollback is immediate and low-risk.

## Known risks

Unchanged from Phase 2 §4: `vercel.json` config correctness (verified against docs, not yet verified against a real Vercel deploy), and the 5 HTML files being the site's highest-traffic surface (mistakes are immediately visible to every homepage visitor). Both are mitigated by the verification already done above; the remaining risk is specifically the real-Vercel-deploy behavior, which can only be confirmed by actually pushing.

## Outstanding

- ☑ All 6 implementation tasks (Phase 2 §2) — done, verified locally
- ☑ Visual/behavioral parity — confirmed across all 5 demos + homepage embed, via mechanical `git diff`, not inspection
- ☑ Local before/after performance measurement — confirmed, large improvement
- ☑ Build reproducibility — confirmed via clean-checkout rebuild, byte-identical output
- ☐ Commit and push to production — **not yet done, awaiting Wael's explicit approval** (this repo has no staging environment; push is directly to production)
- ☐ Post-deploy verification (production Lighthouse re-test, Vercel deploy log check, real-user responsiveness check, all 5 demos functional on production homepage)

## Phase 6 — Technical Review (After Coding)

**Approved 2026-07-07.** Reviewed: Git Diff, Changed files, Architecture impact, Regression risk, Isolation, Test coverage (full request and content in session record). Outcome: APPROVE.

## Phase 7 — Testing

Per `AI_DEVELOPMENT_GOVERNANCE.md` §3, Phase 7 requires manual validation beyond automated tests, explicitly for: Voice, Phone, Geofencing, Notifications, Screen behavior, Permissions, Background execution, End-to-end integrations. **None of these apply to `mynaavi-website`** — confirmed again here, same as the Phase 2 §5 regression table (no backend, no connection to any of those systems).

What Phase 7 consists of for this specific change, stated explicitly rather than left implicit:

**Completed, pre-push (all of Phase 4/5's manual verification, listed above):** visual parity, console errors, network requests, Brakes Web Audio, homepage embed rendering, local Lighthouse before/after, build reproducibility.

**Cannot be completed pre-push — a structural fact about this repo, not a shortcut:** this repo has no staging environment (per `CLAUDE.md`: "Vercel auto-deploys `origin/main` directly to mynaavi.com"). There is no intermediate environment to test the *actual deployed* behavior against before it's live. Four checks are therefore only possible **after** Phase 8 (the push itself):
1. Vercel deploy log — confirm `vercel.json`'s `framework`/`buildCommand` config actually prevents an unintended auto-build, as opposed to only being correct on paper.
2. Lighthouse against the real production URL (mynaavi.com), not local dev — confirms the improvement holds outside this machine's environment.
3. Real-user responsiveness (immediate-scroll check) on the live page.
4. All 5 demos functioning correctly embedded on the live production homepage.

**This is not Phase 7 being skipped** — it's Phase 7 split by structural necessity into a pre-push portion (done, above) and a post-push portion (items 1-4, which requires Phase 8 to have already happened to even be checkable). Flagging this explicitly rather than silently treating "push" as satisfying Phase 7 on its own.

## Phase 8 — Merge (push to production)

**Done 2026-07-07.** Commit `9bae1af` on `origin/main` (`munk2207/mynaavi-website`), pushed directly to production per this repo's no-staging deploy model. Files committed exactly matched the staged approved list (`.gitignore`, `babel.config.json`, `build-demos.js`, `dist/*` × 6, `package.json`, `package-lock.json`, `src/*` × 6, `tailwind.config.js`, and the 5 HTML files + `vercel.json` modifications) — confirmed via `git status --porcelain` immediately before commit.

## Phase 7 — remaining 4 checks, completed post-push against live production

1. **Vercel deploy log** — no CLI auth available in this session to read the log directly. Verified indirectly instead: `curl` against `mynaavi.com/dist/notes-demo.compiled.js` and `mynaavi.com/dist/demos.css` both return `200 OK`; the live HTML now serves `<link rel="stylesheet" href="dist/demos.css">` and `<script src="dist/notes-demo.compiled.js">` with zero `babel.min.js`/`cdn.tailwindcss.com` references — the deploy demonstrably succeeded and picked up the new commit. This is not equivalent to reading the actual build log, flagged honestly rather than claimed as done.
2. **Lighthouse against live production**, same methodology as every prior run in this investigation:

   | Metric | Before (original production) | After (production, F14 live) |
   |---|---|---|
   | Performance score | 28 | **75** |
   | Total Blocking Time | 2,820 ms | **0 ms** |
   | First Contentful Paint | 8.5 s | **3.4 s** |
   | Largest Contentful Paint | 8.8 s | **4.3 s** |
   | Speed Index | 8.5 s | **5.0 s** |
   | Time to Interactive | 11.8 s | **4.3 s** |
   | Total page weight | 1,243 KiB | **546 KiB** |

   Total Blocking Time reaching 0ms is the direct, real-world confirmation of Phase 1's root-cause diagnosis (Babel Standalone was ~86% of all script execution time) — removing it didn't just reduce the cost, it eliminated the entire category of blocking.
3. **Real-user scroll responsiveness** — navigated to the live `mynaavi.com` homepage, issued `window.scrollTo({top: 500, behavior: 'instant'})`, confirmed `window.scrollY` reflected the scroll within a 100ms check. Consistent with 0ms Total Blocking Time: nothing is competing with the scroll on the main thread.
4. **All 5 demos functional on the live production homepage** — inspected each `iframe.story-demo-frame`'s `contentDocument` directly: all 5 have a populated `#root` and correct, distinct header text (`RECORD A THOUGHT`, `BRAKE SERVICES`, `LAYLA`, `DOCTOR VISIT`, `INSURANCE`), confirming each demo mounted and rendered correctly in production, not just that the iframe element exists in the DOM.

**All Phase 7 items now complete.** F14 is fully implemented, reviewed (Phase 3 and Phase 6, both approved), evidenced, tested, and live in production.

## Revision history

- **2026-07-07, first draft.**
- **2026-07-07, this revision** (per external review): performed the build-reproducibility check that Phase 2 §2 flagged as a forward-looking note rather than leaving it as "scheduled" — clean-checkout rebuild confirmed byte-identical to the original build. Corrected "byte-identical" from an inspection-based inference to a `git diff`-verified claim, per the reviewer's wording concern — ran the diff on all 5 HTML files and confirmed no line differs beyond the intended CDN/script swap.
