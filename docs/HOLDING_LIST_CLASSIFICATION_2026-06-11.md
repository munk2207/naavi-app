# Holding List Classification — 2026-05-08

Research-and-planning session output. Walks the 26-item holding list from `docs/SESSION_HANDOFF_2026-05-07_V57.13.7_BUILD_165.md` and classifies each item.

## Classification scheme

Four lists, each with the same column shape (`ID | Description | Surface | Notes | Server/AAB`):

- **Bugs (B)** — broken or incomplete behavior on a user-facing surface
- **Features (F)** — new user-facing capabilities
- **Tooling (T)** — internal dev / test / measurement infrastructure
- **Ideas (I)** — brainstorming-stage entries; deferred-by-design or path-not-chosen items not yet committed as real features

**Severity** — encoded in the ID: 1 = top, 2 = medium, 3 = low. Letter suffix (`a`, `b`, `c`…) disambiguates within a severity tier (e.g., B1a, B1b, B1c are all top-severity bugs).

**Server/AAB** — where the work lands:
- `Server` — Edge Function, SQL, voice server, web; no AAB build required
- `AAB` — mobile code; requires `npx eas build --auto-submit`
- `Both` — server + mobile pieces

**Surface** — which user-facing surface owns the work; used for cross-surface drift discipline (see CLAUDE.md Rule 16). Values:
- `mobile` — work lands on mobile codebase only; no voice change required
- `voice` — work lands on voice-server codebase only; no mobile change required
- `both` — both surfaces; parity required (when one ships, the other must follow before drift)
- `backend` — shared backend (Edge Functions / SQL); both surfaces benefit automatically
- `website` — `mynaavi-website` repo only; neither mobile nor voice

**Architectural principle (Wael 2026-05-08):** every queryable channel = background sync at per-channel depth + live-overlay at question-time. Applies to calendar, email, SMS, WhatsApp.

**Cross-surface drift mechanism:** classification's `Surface` tag is best-effort interim discipline. The mechanical guarantee comes from Voice Completion Roadmap W2 (Anthropic Structured Outputs) + W3 (Voice Automated Regression Suite) — see `docs/VOICE_COMPLETION_ROADMAP_2026-05-08.docx`. Until W2 + W3 land, Surface column + CLAUDE.md Rule 16 (`parity-impact:` on commits) are the human-discipline net.

---

## Bugs (B) — OPEN

| ID  | Description                         | Surface | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Server/AAB | Status                      |
| --- | ----------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------- |
| B9a | Ambiguous channel/action_type verbs silently default instead of asking — violates deterministic-design principle (2026-07-09) | backend | **Found incidentally while designing F15's Hypothesis Validation test phrasings.** `supabase/functions/get-naavi-prompt/index.ts:481-485` (`DRAFT_MESSAGE` channel rule): “Ambiguous (e.g. “send”, “write”, “tell”) → default “email”” — a silent default, not a clarifying question. Directly conflicts with `project_naavi_deterministic_design` (“DECIDED — Claude = translator only; Robert picks, Naavi never guesses”). Separately, the location-alert tools (`set_location_rule_address`/`set_location_rule_chain`) have no general rule for ambiguous verbs at all — only one worked example (`get-naavi-prompt/index.ts:1101`, “Tell my wife...” → `action_type='sms'`) that is inconsistent with the `DRAFT_MESSAGE` rule's “tell → email” default, so actual behavior for an ambiguous verb on a location alert is unverified either way. **Confirmed by Wael (2026-07-09) as a major task, not a quick fix.** No Phase 1 doc written yet — scope (which tools/rules are affected, what “ask instead of guess” should look like in conversation) needs its own investigation, separate from F15 (F15 is about recipient extraction; this is about channel/action_type selection never guessing). | Server | open — not yet scoped, needs its own Phase 1 |
| B9b | Contact-info query returns email when phone number was specifically asked for (2026-07-09) | mobile | **Found incidentally while diagnosing F15/B9a.** Live repro on staging: “What's Bob's phone number” → Naavi replied “Bob — aggan2207@gmail.com” (his email, not his phone `(343) 333-2567`, both present on the contact card). “What's Bob's contact info” correctly returns the email; asking specifically for the phone number should return the phone number, not repeat the email. Likely the contact-query response formatter (global-search `contacts` adapter or its answer-rendering step) always surfaces one field regardless of which field the question asked for. **Related to [[B9a]]** (both are “Naavi picks a channel/field without checking what was actually asked/specified” bugs) but a different code path — B9a is the ambiguous-verb action_type classifier in `get-naavi-prompt`; this is contact-info Q&A formatting. Not yet scoped, no Phase 1 written. | Server | open — not yet scoped |
| B9c | Disabled list still shows as active on the "Your Lists" overview screen (2026-07-09) | mobile | **Live repro on staging.** Disabled the “work” list via the app's Disable-alert-style confirm flow (“Disable ‘work’? The list will be hidden…”). Checked the `lists` table directly right after — `enabled: false` persisted correctly (`id: eb565c12-dae0-4b81-93a3-3f8b02b925a6`, `updated_at` matches the action). **So this is confirmed NOT a write-path/backend bug** — the DB is correct. But the “Your Lists” screen's “All (3)” tab still showed `work` with no visual distinction from the two genuinely-enabled lists (`cottage`, `grocery`) — either the screen isn't filtering/flagging on `enabled` at all, or it's rendering stale cached state instead of re-fetching after the disable mutation. Separately, the disabled list's own detail view showed “No items yet” — lists store items in a backing Google Doc (`drive_file_id`), not inline on the row, so this may be “app doesn't fetch doc contents for a disabled list” rather than lost data; not yet investigated. Lower risk than a write-path bug once scoped — read/render only. No Phase 1 written yet. | AAB | open — not yet scoped |
| B9d | Intermittent bad safe-area insets can persist for minutes, freezing/misplacing UI (e.g. 3-dot menu), historically "fixed" by force-stopping the app (2026-07-09) | mobile | **Confirmed root-cause candidate for a previously-known, previously-unsolved bug.** Commit `60b61a1` (2026-05-17, V57.19.0 build 192) already documented this exact symptom from Wael's own report: “screen draws behind the Android nav bar until I force-stop the app” — and added `<InsetDiagnostic />` (`app/_layout.tsx:393`) specifically to catch it “next time.” **This session was that next time.** Direct evidence from `client_diagnostics.inset-snapshot`: normal portrait insets are consistently `{top:26.31,left:0,right:0,bottom:48}`. Four times on 2026-07-09 a landscape-shaped reading briefly appeared instead — `{top:29.87,left:48,right:26.31,bottom:0}` — looking like a 90°-rotated read while the device stayed in portrait. Three of the four self-corrected within 1-3 seconds (03:50:10→03:50:13, 06:43:00→06:43:01, 17:14:28→17:15:29). **The fourth, at 13:16:29, did not self-correct** — no further `inset-snapshot` change logged for 16 minutes (until 13:32:24, itself showing a `bottom:0`→`bottom:48` flicker before settling). This 16-minute window directly overlaps Wael's own live report: screen flickering while typing to create an alert, forced to scroll to see Naavi's answer (confirmed via direct questioning to have happened in this same window, though exact minute uncertain — app was fully idle 06:45-13:16 so 13:16:29 is the first activity after that gap and the best-evidenced match), followed shortly after by the 3-dot menu becoming unresponsive, which Wael resolved by force-stopping and relaunching the app (`lifecycle-boot` cold-start marker at 13:36:18 confirms the relaunch). **Trigger not yet identified** — leading candidates not yet distinguished: (a) app-resume-from-background safe-area recalculation race in `react-native-safe-area-context`, (b) keyboard show/hide during message composition, (c) some other screen/navigation transition. **Downstream consequence, separately significant:** the subsequent force-stop is suspected (not proven — see [[project_naavi_f15_geofence_forcestop]] memory / this session's transcript) to have interfered with background geofence delivery on the immediately-following drive attempt (all 3 test alerts failed to fire); a same-day retest without force-stop involved succeeded 3/3. **Not yet scoped as its own Phase 1** — logged per Wael's explicit "report for future investigation" instruction (2026-07-09), not investigated further this session. | AAB | open — confirmed reproduction with evidence, root cause/trigger not yet identified |
| B9e | F15 Defect A display fix: Alerts screen shows the specific self-override address instead of generic "all channels" copy (2026-07-09) | mobile | **Code complete, not yet deployed — needs a new staging APK.** `app/alerts.tsx::formatWhatHappens` now calls out `self_override_email`/`self_override_phone` specifically (e.g. "Naavi will email you at fatma@egyptiancan.com. Other channels (text, WhatsApp, push, voice call) reach you normally.") instead of the generic self-alert copy that doesn't surface the override address — found live during F15 Defect A validation (2026-07-09) when Wael compared it against how third-party alerts already show their specific destination. **Data/dispatch correctness for Defect A is already verified and live on staging** (server-side fix, no APK needed) — this is purely the display layer catching up. **Explicitly batched, per Wael (2026-07-09): do not build a standalone APK for this — combine with any other AAB-only fixes into one dedicated build session.** | AAB | code complete — queued for next APK build, not yet deployed |
| B9f | `evaluate-rules`'s `callVoice` never writes to `sent_messages`, unlike `report-location-event`'s `callVoice` which does (2026-07-09) | backend | **Found while validating F15 Defect A's evaluate-rules fix.** A time-triggered self-alert with voice_call enabled fired correctly and Wael directly confirmed receiving the call, but no corresponding row appeared in `sent_messages` the way SMS/WhatsApp rows for the same fire did. Traced to `supabase/functions/evaluate-rules/index.ts`'s `callVoice` function lacking the `admin.from('sent_messages').insert({channel:'voice',...})` call that `report-location-event`'s `callVoice` has (added there 2026-05-23 per its own "V57.10.2 — track voice calls in sent_messages" comment). Same class of gap as `send-user-email` never logging email sends (already known) — delivery is real, just not database-observable via the normal audit trail for this one function+channel combination. Not fixed as part of F15 (unrelated to recipient extraction/self-override); logged for its own future session. | Server | open — not yet scoped |
| B9g | Time-triggered, phone-number-based self-override (SMS/WhatsApp/voice) can be misrouted to an unrelated contact — confirmed live, root cause traced (2026-07-09) | mobile | **Found live-testing F15 §1.2.2's four-field split.** "WhatsApp me at +16137976746 in 3 minutes say hello" produced "I'll text Laura at +1 343-333-2567" — wrong contact, wrong channel (SMS not WhatsApp), wrong number entirely. **Confirmed via `client_diagnostics`:** Layer 2 correctly extracted `self_override_whatsapp: "+16137976746"` but *also* incorrectly set `to_name: "Laura"` in the same params (classifier prompt explicitly forbids populating both). The contaminating context, present in the userText Haiku/Claude actually saw: `## Contact found for 16137976746 / Name: Laura / Phone: +1 343-333-2567` — traced to `hooks/useOrchestrator.ts` lines 2014-2030, a pre-existing, unrelated feature that scans every message for any 10-digit phone pattern and injects a "Contact found" block if a contact matches, with zero awareness of self-override ("me"/"myself") phrasing. The matched contact's on-file number doesn't equal the searched digits, so there's a second, separate bug in that lookup itself (loose match, or a multi-number contact showing the wrong number) not yet root-caused. **Second, separate confirmed gap:** `hooks/useOrchestrator.ts` has zero references to `self_override` anywhere (grepped) — its `SET_ACTION_RULE` handler (lines 3230-3295) resolves `action_config.to` via `resolve-recipient` whenever non-empty, with no check for `self_override_*` presence first. This file was explicitly named in `docs/F15_PHASE2_CHANGE_PLAN_2026-07-09.md` §1.4 as needing self-override-aware forwarding, but that change was never implemented. **Scope, corrected after Wael caught an overstatement:** NOT all time-triggered self-overrides are unreliable — `self_override_email` on the same Claude+tools path was already live-validated working cleanly earlier the same session ("Email me at aggan2207@gmail.com in 3 minutes saying test", 19:04:45 EST, no contact-lookup injection at all since email addresses don't match the phone-lookup's regex). The defect is narrowly scoped to phone-number-shaped self-overrides (`self_override_sms`/`self_override_whatsapp`/`self_override_voice`) on the time-trigger path, and only when the override number happens to match (correctly or spuriously) an existing contact. Location-trigger requests are unaffected regardless of channel (Layer 2 handles those directly and stayed clean in tonight's tests, despite seeing the same injected context). **Not yet tested:** self_override_voice (test 3, same phone-based/time-trigger shape, expected to reproduce the same contamination but not yet confirmed) and the no-override regression case (test 5). No Phase 1 opened yet — Wael's instruction was to finish live testing first, formalize after.

**2026-07-10 independent re-verification, direct DB query (`action_rules`/`sent_messages`/`client_diagnostics` on staging), no code changed:** (1) **Correction to severity** — test 2 never actually committed anything. Zero `action_rules` rows with `trigger_type='time'` exist for all of 2026-07-09; zero `sent_messages` to the override number, to Laura, or with a "hello" body; zero `client_diagnostics` mentioning "Laura." The quoted "I'll text Laura at +1 343-333-2567" is Turn-1 **confirm speech only** (exact template match: `naavi-chat/index.ts:2901`) — Wael did not say yes, so no rule was written and nothing was sent. The bug is real and would have caused an actual third-party misdelivery had it been confirmed, but did not this time. (2) **Retraction** — the "second, separate bug… matched contact's on-file number doesn't equal the searched digits" claim above is wrong. Wael's native Android Contacts app confirms Laura's on-file mobile number genuinely is +1 343-333-2567, an exact match. No data-integrity bug in the contact lookup. (3) **Root-cause mechanism refined** — the framing above (useOrchestrator's `SET_ACTION_RULE` handler at lines 3230-3295 as the resolution site) does not fire for this bug: Layer 2's own deterministic time-trigger handler in `naavi-chat/index.ts` (lines 2797-2911, reached via the `trigger_type==='time'` fallthrough at line 1849) resolves the recipient and `return`s its own PENDING_INTENT before Claude, the T1/T2 intercepts, or the client are ever reached. That handler (a) never reads `self_override_*` anywhere, (b) rebuilds `action_config` from a literal object (`{ body, to_name, to_phone: '' }`, line 2884) that drops any `self_override_*` the classifier extracted rather than just deprioritizing it, and (c) branches only email-vs-SMS (`_ftIsEmail`, lines 2807-2873) with no WhatsApp/voice branch at all — explaining the wrong-channel symptom independent of the contamination. **Diagnostic instrumentation added, code complete, not yet deployed — needs a new staging APK, batched per Wael's "no APK just for this" instruction (2026-07-10), same pattern as B9e:** `lib/contacts.ts::lookupContactByPhone` now takes an optional `diagSession` and logs `phone-lookup-start`/`phone-lookup-matched` (which of the 4 query-format variants hit)/`phone-lookup-no-match`; `hooks/useOrchestrator.ts` passes `diagSession` through and logs `phone-lookup-context-injected` with the exact text block appended to the user's message. Will confirm or rule out the still-unproven "5-char phonetic-prefix fallback in `lookup-contact`'s `searchContacts` call" hypothesis for how a 613-area-code query ever matched a 343-area-code contact on the next reproduction. Typechecked clean (`npx tsc --noEmit`, no new errors).

**2026-07-10, same session, continued — Wael pulled the actual Supabase Dashboard Edge Function logs for `lookup-contact` from the night of the bug (still within retention).** This surfaced a genuinely new, separate, confirmed bug and partially resolved the phonetic-fallback hypothesis. **Confirmed:** 3 of the 4 automatic query-format variants `lookupContactByPhone` builds were malformed — `digits = phone.replace(/\D/g,'')` never stripped a leading NANP country-code "1" when the input already included it (e.g. "+16137976746" → `digits="16137976746"`, 11 digits instead of 10), corrupting every variant built from it: `+1${digits}` doubled the "1" (`"+116137976746"`), and the spaced-format slice offsets shifted every group by one digit (`"+1 161 379 76746"`). Live logs confirm all 3 corrupted variants returned 0 results (including through `lookup-contact`'s own phonetic fallback) — **ruling out the phonetic-fallback hypothesis for those 3 paths.** The successful "Laura" match came from the 4th, uncorrupted variant (`phone.trim()`, i.e. the number exactly as typed) — but `lookup-contact` only logs the query string on its failure path, never on success, so the exact mechanism connecting a 613-area-code query to a 343-area-code contact is still not pinned down. **Fixed and verified this session (code complete, typechecked clean, not yet deployed — same APK batching as above):** (a) `lib/contacts.ts::lookupContactByPhone` now strips a leading "1" from an 11-digit `digits` before building variants; (b) `hooks/useOrchestrator.ts`'s phone-detection was rewritten from a fixed-width regex (which silently TRUNCATED any longer international number into a wrong 10-digit fragment, e.g. "+447911123456"→"4479111234" — a second, independently confirmed bug, see [[B9h]]) to a digit-run approach that only proceeds on a recognized NANP shape (10 digits, or 11 with leading "1", stripped) and skips injection entirely otherwise. Re-verified against the original test 2 phrase and test 1's SMS override — both still resolve to the correct clean number. **Important scope note: neither fix resolves B9g itself.** They fix a genuinely separate defect (malformed lookup queries) that was contributing noise to this investigation. The actual B9g misrouting mechanism — Layer 2's `naavi-chat/index.ts:2797-2911` never reading `self_override_*` and rebuilding `action_config` from a literal that drops it — remains completely unfixed. Phase 1 still not opened.

**2026-07-10, same session, continued — retested test 2 live (identical phrase, nothing deployed yet) to gather the missing evidence.** Reproduced identically ("I'll text Laura at +1 343-333-2567..."), as expected since nothing had shipped. Fresh Dashboard logs (this time within a known 2-minute window, no retention risk) finally captured the smoking gun: the 4th query variant, `"+1 161 379 76746"` (the spaced format, still malformed pre-fix), got 0 exact results and triggered `lookup-contact`'s phonetic fallback. That fallback does `name.trim().split(/\s+/)[0]` — for a query with a space right after `"+1"`, this extracts just **`"+1"`** (2 characters) as the retry term, an almost-universal prefix that matched 3 unrelated contacts; the code took the first one, "Laura." **This is the confirmed, complete root cause of the contamination — not the earlier unproven hypothesis, an actually-observed mechanism.** Critically, this flaw is independent of the digit-corruption bug fixed earlier this session: the spaced-format variant always has a space right after `"+1"` by construction, so even the already-fixed client code would still hit this same fallback path whenever none of the 4 exact-format queries match (the normal case for any number not saved verbatim in a contact). **Fixed and deployed to staging (Edge Function, no APK, live now):** `supabase/functions/lookup-contact/index.ts` — the phonetic fallback now skips entirely when the query is phone-shaped (digits/spaces/dashes/parens/plus only, regex `/^[\d\s\-().+]+$/`), since a name-typo heuristic never made sense for a phone number; a real phone lookup now fails cleanly (not found) instead of guessing. Deploy confirmed successful (`supabase functions deploy lookup-contact --project-ref xugvnfudofuskxoknhve`). **Not yet retested against this deploy** — next step. **Possible but unconfirmed bonus effect:** since Layer 2's broken `naavi-chat/index.ts:2797-2911` handler only activates when `to_name` is non-empty, and `to_name` was only getting set because of this contamination, removing the contamination may mean this specific test case now bypasses that broken handler entirely (falls through to Claude's own tool-use path via the `!_ftToName` self-reminder check at line 2813) rather than fixing the handler itself. The handler's own defects (no WhatsApp/voice branch, drops `self_override_*` on rebuild) remain completely unfixed and would still misfire for any other case where `to_name` legitimately gets set.

**2026-07-10, same session, continued — retest against the deployed fix, live, full end-to-end confirmation.** Ran the identical test 2 phrase a third time. Result: clean confirm ("I'll send you a WhatsApp message at +16137976746 in 3 minutes saying 'hi'..."), no Laura, correct channel named. Confirmed with "yes." **Verified all the way through fire:** `action_rules` row created with `action_config: {"self_override_whatsapp":"+16137976746", "to_phone":"+13433332567", ...}` — note `to_phone` reappeared (a new, different contamination artifact worth its own look — see below), but traced `evaluate-rules/index.ts` lines 952-978 and confirmed `hasSelfOverride` is checked before any address-matching and the self-alert branch's per-channel target selection (`selfWhatsappTarget = selfOverrideWhatsapp || userPhone`) never reads the contaminated `to_phone` at all — it's dead data in this path. **Fire-time evidence, pulled directly from `evaluate-rules` Edge Function logs (Dashboard):** `Rule c528c7cc-... fan-out (self): sms=ok email=ok voice-call=ok — 3/3 ok`. Cross-checked against user_settings.alert_channels_enabled = `["sms","email","voice_call"]` (WhatsApp and push not enabled) and against what Wael actually received on-device (call, SMS, email — no WhatsApp, no push) — exact match. **No bug in this fire.** WhatsApp correctly did not send because it was never in the user's enabled-channels list — not a Twilio failure (an earlier hypothesis this session, now retracted after the log showed WhatsApp wasn't even attempted) and not a settings-changed-after-the-fact issue (a second earlier hypothesis, also retracted — `user_settings.updated_at` is a row-level timestamp shared by every field, not proof the channel list itself changed at that moment; the fan-out log is what actually settled it). **B9g's original contamination bug is now confirmed fully fixed and verified end-to-end** for the phone-shaped self-override / time-trigger case. **Still open, not yet investigated:** the reappeared `to_phone: "+13433332567"` field in this rule's `action_config` — confirmed inert for delivery (traced above) but still a data-quality artifact worth root-causing separately; likely a different manifestation of the same underlying "phone number in message → something resolves a contact and writes it somewhere in action_config" pattern as the original bug, just landing in a different field this time. Layer 2's `naavi-chat/index.ts:2797-2911` handler itself (no WhatsApp/voice branch, drops `self_override_*` on rebuild) also remains completely unfixed — untouched by any of tonight's work — and would still misfire for any case where `to_name` legitimately gets populated (e.g. a genuine third-party time-trigger request). No Phase 1 opened. | Server + mobile (`hooks/useOrchestrator.ts`, `lib/contacts.ts` — queued for APK; `lookup-contact` — deployed to staging, verified working) | contamination bug CLOSED (verified end-to-end); stray `to_phone` artifact and Layer-2 handler itself both still open, unscoped, Phase 1 not opened |
| B9h | Phone-number handling throughout the app assumes North American (NANP) format — international numbers are unsupported, and were previously mishandled silently rather than rejected (2026-07-10) | mobile + backend | **Found while scoping [[B9g]]'s fix — Wael flagged that many people living in the US/Canada keep a foreign mobile number (e.g. UK +44) as their primary line, so this isn't an edge case.** Confirmed live (Node regex test, not inference): the phone-detection regex in `hooks/useOrchestrator.ts` was hardcoded to a fixed 10-digit (or 11-with-leading-"1") shape and, for any longer international number, did not fail to match — it silently TRUNCATED into a wrong, shorter number instead (`+447911123456` → `4479111234`; `07911123456` (UK local format) → `0791112345`). That truncated garbage then drove a real contact lookup and could spuriously match an unrelated contact, i.e. a more severe version of the same failure mode as [[B9g]]. **Fixed and verified this session, code complete, not yet deployed (same APK batching as B9g):** rewrote the detection to find the full digit run and only proceed on a recognized NANP shape (10 digits, or 11 with leading "1"); anything else is left alone — no injection, no guess — rather than truncated. **What is explicitly NOT fixed or verified, three separate open gaps:** (1) **Claude/Haiku extraction accuracy for non-NANP numbers is untested** — whether `self_override_whatsapp`/`self_override_sms`/etc. correctly capture an international number as literal text from a user's message has never been tested; this is the classifier's own text understanding, a different code path from the regex fixed here. (2) **Twilio international delivery is unverified** — international SMS/WhatsApp/voice typically requires separate account-level geographic permissions on Twilio, not just a correctly-formatted API call; the current account's international messaging configuration has not been checked. (3) **Broader NANP-hardcoding pattern found but not audited** — a grep for `+1` string-building / 10-digit-fixed-width patterns turned up hits in 7 Edge Functions (`evaluate-rules`, `naavi-chat`, `global-search`, `naavi-spend-summary`, `assistant-fulfillment`, `list-contact-names`, `text-to-speech`); `resolve-recipient`'s own phone detection (`looksLikePhone`, line 53-56) was spot-checked and is NOT NANP-hardcoded (correctly accepts any `+`-prefixed or 7+-digit string), but the other 7 files have not been individually checked and may or may not have their own version of this bug. Connects to the existing `feedback_global_first` standing rule (build every feature as global from day one) — this whole area was NANP-only by default, in violation of that rule. No Phase 1 opened — scope (full international number support vs. just closing unsafe-guess failure modes) needs its own decision before further work. | Server + mobile (`hooks/useOrchestrator.ts` fixed; `resolve-recipient` spot-checked clean; 7 other Edge Functions unaudited) | open — truncation bug fixed (queued for next APK), 3 broader gaps identified, not scoped, Phase 1 not opened |
| B9n | Two related bugs found live in production after B9i/j/k/l closed: (1) Claude/Haiku can populate a self_override_* field AND to_name in the same params, contaminating the confirm speech and stored rule with a third party's name; (2) any action command containing a phone number (7+ digit run) was mistakenly treated as a retrieval/lookup query, running a pre-search contact search against the raw action text and surfacing an unrelated contact as a "Results for..." card (2026-07-13) | mobile | **Bug 1, found live:** "WhatsApp me a message to +16137976746 in 3 minutes say hi" — Layer 2 correctly set `self_override_whatsapp:"+16137976746"` but ALSO set `to_name:"Fatma Elmehelmy"` (a REAL contact whose actual saved phone number happens to be the same digits as the test number used throughout this session) — same dual-population contamination pattern as the original B9g, but this time matching a genuinely real contact instead of a wrong one. Claude then framed the confirm speech around the third party ("I'll text Fatma Elmehelmy...") instead of the self-override. Traced to the exact gap B9g's original write-up flagged but never fixed: `hooks/useOrchestrator.ts`'s `SET_ACTION_RULE` handler resolved `to`/`to_name` via `resolve-recipient` whenever non-empty, with no check for `self_override_*` presence first. **Fixed:** added a `hasSelfOverride` check that (a) skips the whole recipient-resolution block when any `self_override_*` field is present, and (b) deletes the stray `to`/`to_name` from `action_config` in that case, so it can't survive into the stored rule and confuse `app/alerts.tsx`'s `detectIsSelf` display logic (which checks `to`/`to_phone`/`to_email` but not `self_override_*` presence). **Bug 2, found live same session:** "send whatsapp esage to +16179746 thee mintes say hel" (a garbled/typo'd message) triggered a pre-search contact lookup against the raw text (because the 8-digit phone number tripped `hasLongDigitRun`, one of the signals for `isRetrievalQuery`), surfacing a completely unrelated contact ("Mario Venditti, Above Security") as a "Results for..." card — even though the actual confirm-ask response was correct that time. **Fixed:** added an `isMessageSendAction` check (matching the existing `isTicketCreation`/`isEmailAlertCreation` false-positive exclusions already in the code) that excludes send/text/WhatsApp/message/email/call verbs combined with "say(ing)", "when I arrive/get", "in N minutes", or "at TIME" phrasing from `isRetrievalQuery` — verified against all 4 live-reproduced phrasings from this session (the garbled one included). Regression tests added: `tests/catalogue/session-2026-07-13-b9n-self-override-priority.ts` (3 tests, source-pattern assertions per the disclosed React-Native-import limitation), all pass; full suite 393/397 green, no regressions. **NOT YET DEPLOYED — both fixes are client-side (`hooks/useOrchestrator.ts`) and require a new staging APK build before they can be tested or take effect on-device.** No Phase 1 opened. | Mobile (`hooks/useOrchestrator.ts`) | code complete, tests pass, queued for next staging APK build — not yet testable on-device |
| B9o | Chat screen scrolls to the TOP instead of staying on the latest answer after certain responses (e.g. viewing the alerts list), instead of staying scrolled to the bottom (2026-07-13) | mobile | **Found live: "the screen roll to the top after every new card, it does not stay focus on the answered question."** Root cause confirmed via code read: `app/index.tsx`'s auto-scroll `useEffect` used `assistantSpeech?.startsWith('Here are your')` to detect the compound-plan breakdown response (which intentionally scrolls to top so item #1 is visible first) — but this loose prefix also matches `LIST_RULES`'s speech template (`naavi-chat/index.ts:217`, `'Here are your alerts.'`), so viewing the alerts list (and any future response starting with the same words) incorrectly scrolled to the top instead of the bottom. Confirmed as a same-day regression, not longstanding: `bd9b386` (Jun 23) originally scoped this to a proper boolean flag (`isCompoundResult`); `f058430` (Jun 23, same day) changed it to the loose string-prefix match that caused the collision. **Fixed:** `isCompoundPlan` now uses the server's own specific regex (`naavi-chat/index.ts:4126`, `/^Here are your \d+ actions:/`, requiring a digit count and "actions:") instead of the loose prefix — verified it still matches a genuine compound-plan template and correctly does NOT match "Here are your alerts." Regression test added: `tests/catalogue/session-2026-07-13-b9o-scroll-to-top-collision.ts` (2 tests, source-pattern assertions per the disclosed React-Native-import limitation), all pass; full suite 395/399 green, no regressions. **Client-side (`app/index.tsx`) — code complete, NOT yet deployed, needs a new staging APK before testable on-device (same batch as B9e/B9h/B9n).** No Phase 1 opened. | Mobile (`app/index.tsx`) | code complete, tests pass, queued for next staging APK build — not yet testable on-device |
| B9p | Geofence setup screen's "Fix" button for Notifications appears to do nothing — user has to manually enable notifications in Android Settings (2026-07-13) | mobile | **Found live on a fresh install of staging build 303.** Confirmed root cause: two independent places requested the same `POST_NOTIFICATIONS` permission and raced each other. `app/_layout.tsx`'s `maybeAutoRegisterPush` ran automatically on every cold start and called `Notifications.requestPermissionsAsync()` whenever status was `'undetermined'` — firing before the user ever saw the setup screen. Android only shows its permission dialog a limited number of times before silently suppressing it and returning the prior status with no dialog at all — so by the time the user tapped "Fix" on the setup screen (`hooks/useGeofencePermissions.ts:113-117`), Android showed nothing, and the code fell through to `openAppSettings()`, which opens the generic app-info page, not the notification toggle — looking like nothing happened. Location, activity, and battery-optimization don't have this problem since nothing else in the app requests those; the Fix button is their only requester. **Fixed:** (1) `app/_layout.tsx`'s auto-register no longer calls `requestPermissionsAsync()` — it only checks status via `getPermissionsAsync()` and registers the push token if already granted, so the setup screen's Fix button is the one place that ever asks; (2) added `openNotificationSettings()` using the `android.settings.APP_NOTIFICATION_SETTINGS` intent (falls back to the generic app-info page on older Android that doesn't support it), so even when the fallback fires, the user lands directly on the notification toggle instead of a confusing generic page. Regression test added: `tests/catalogue/session-2026-07-13-b9p-notification-permission-race.ts` (2 tests, source-pattern assertions per the disclosed React-Native-import limitation), all pass; full suite 397/401 green, no regressions. **Client-side (`app/_layout.tsx`, `hooks/useGeofencePermissions.ts`) — code complete, NOT yet deployed, needs a new staging APK before testable on-device.** No Phase 1 opened. | Mobile (`app/_layout.tsx`, `hooks/useGeofencePermissions.ts`) | code complete, tests pass, queued for next staging APK build — not yet testable on-device |
| B9q | Chat screen jumps toward the top of the whole conversation on every spoken response (weather, time, calendar — any answer), not just the LIST_RULES/compound-plan case (B9o) (2026-07-13) | mobile | **Found live during B9o retest** — Wael explicitly ruled out alerts as the trigger: reproduced with plain queries ("What's the time now", "What's on my calendar this week", "What's the whether", etc.), confirming a separate, more general bug. Root cause: `app/index.tsx`'s TTS "chunk-scroll sync" effect (follow-along-as-Naavi-speaks) computed its scroll target as a fraction of `scrollContentHeight` — the rendered height of the **entire conversation** — with the first chunk hardcoded to `y: 0`, the absolute top of the whole chat. This only ever lands correctly on the latest response near the very start of a fresh conversation; for any later turn, every new spoken response jumped toward the top of the whole chat history instead of following the response actually being read. **Fix — explicitly chosen as "the screen should follow the conversation, no faster or slower" (option to properly fix, not just disable):** added `latestTurnLayoutRef`, populated via `onLayout` on the latest turn's wrapper View (scoped to `isLatest` only), capturing that turn's own on-screen position + height. The chunk-sync effect now scrolls relative to that — chunk 0 lands at the top of the CURRENT response, later chunks track proportionally within its own height, not the whole conversation's. Removed the now-dead `scrollContentHeight` state/prop entirely rather than leaving it unused. Regression test added: `tests/catalogue/session-2026-07-13-b9q-tts-scroll-sync.ts` (3 tests, source-pattern assertions per the disclosed React-Native-import limitation), all pass; full suite 400/404 green, no regressions. **Client-side (`app/index.tsx`) — code complete, NOT yet deployed, needs a new staging APK before testable on-device (same batch as B9e/B9h/B9n/B9o/B9p).** No Phase 1 opened. | Mobile (`app/index.tsx`) | code complete, tests pass, queued for next staging APK build — not yet testable on-device |
| B9r | Mitigation for B9m — Google People API search doesn't reliably return the same result on every call, causing a real contact to spuriously resolve as "not found" (2026-07-14) | mobile | **Reproduced live a second time same session, reinforcing B9m's diagnosis:** identical query for a real, existing contact ("Xxx") returned `not_found` from `resolve-recipient` once, then resolved cleanly on an identical retry minutes later — no code change in between, confirming the inconsistency is on Google's side. **Fix, per Wael's explicit design:** `hooks/useOrchestrator.ts`'s `SET_ACTION_RULE` recipient resolution now retries `resolve-recipient` once, silently, ~2.5s after a `not_found` result, before telling the user anything — in the common case (retry succeeds) the user never sees an error, just a slightly longer "thinking" pause. Retry is scoped to `not_found` only (not `ambiguous`/`invalid`, which a retry can't fix). If the retry ALSO comes back `not_found`, the message was rewritten from a flat "I don't have a contact named X" (which may not be true) to an honest "I'm having a technical hiccup finding X — try again in a moment, or tell me their email or phone number directly" — deliberately avoids claiming a specific unverified cause (e.g. "network problem") per CLAUDE.md's outbound-claims rule; Wael explicitly confirmed generic "technical" framing over blaming a specific cause we haven't verified. Regression test added: `tests/catalogue/session-2026-07-14-b9r-contact-lookup-retry.ts` (3 tests, source-pattern assertions per the disclosed React-Native-import limitation), all pass; full suite 403/407 green, no regressions. **Client-side (`hooks/useOrchestrator.ts`) — code complete, NOT yet deployed, needs a new staging APK before testable on-device (same batch as B9e/B9h/B9n/B9o/B9p/B9q).** Scoped to this one call site (location-alert third-party recipient resolution) — other `lookup-contact`/`resolve-recipient` call sites (time-trigger third-party, `MAKE_CALL`, `DRAFT_MESSAGE`) have the same underlying B9m exposure but were not extended in this pass; worth revisiting if the same symptom is seen there. No Phase 1 opened. | Mobile (`hooks/useOrchestrator.ts`) | code complete, tests pass, queued for next staging APK build — not yet testable on-device; mitigates one of several exposed call sites |
| B9m | A saved contact's email/phone/name text can genuinely collide with an unrelated name query (e.g. contact "MyNaavi" with email `wael@mynaavi.com` matched a search for "wael"), and Google People API's `searchContacts` cache is not always consistent across identical calls, so only one of two genuinely-matching contacts may surface non-deterministically (2026-07-13) | server/infra | **Found live-testing after B9i/j/k/l closed.** "Send sms message to wael when i arrive to 510 Bayview dr" resolved to a contact named "MyNaavi" (phone = Naavi's own Twilio number) instead of the real "Wael" contact. Confirmed root cause via direct data checks: the "MyNaavi" contact (created per onboarding doc instructions, `docs/NAAVI_CLIENT_ONBOARDING.md:28`, "Save the MyNaavi phone number in your contacts") has email `wael@mynaavi.com` — a genuine substring match for the query "wael". A fresh identical query minutes later correctly returned only the real Wael contact, and renaming the real contact to a non-colliding name ("Xxx") resolved cleanly and reliably (confirmed by Wael, retested and passed) — both consistent with Google's documented search-cache inconsistency rather than a Naavi code bug. **Reproduced a second time, 2026-07-14** — the same "Xxx" contact spuriously returned `not_found` once, then resolved cleanly on an identical retry minutes later, with zero code changes in between — this second, independent instance reinforces the Google-search-cache diagnosis rather than a one-off fluke. **Partial mitigation shipped as B9r** (see below): the location-alert third-party recipient path now silently retries once before surfacing an error, absorbing this exact symptom for that one call site. **Still not fixed at the root** — the original "MyNaavi"-collision proposed mitigation (block any contact matching Naavi's own known service numbers from ever resolving as a recipient) remains unimplemented, and other `lookup-contact`/`resolve-recipient` call sites don't yet have B9r's retry-once mitigation either. No Phase 1 opened. | Server (`resolve-recipient`/`lookup-contact`) — root cause is Google People API's own search-cache behavior interacting with a real, user-created contact's email field | open — root cause confirmed (reproduced twice), one call site partially mitigated (B9r), broader fix (service-number block, retry extended to other call sites) not implemented, no Phase 1 opened |


---

## Features (F) — OPEN

| ID   | Description                                                                                                                                                                                                        | Surface | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Server/AAB                                    | Status                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | ----------------------------------------------------------------------------- |
| F11a | Demo scenario content rebuild — align with Naavi's self-described top 5 capabilities (2026-07-04)                                                                                                                  | voice   | **STATUS 2026-07-05: shipped to STAGING, field test pending.** Phase 1 (`docs/F11A_PHASE1_PROBLEM_DEFINITION_2026-07-04.md`), Phase 2 (`docs/F11A_PHASE2_CHANGE_PLAN_2026-07-04.md`, 3 rounds of external review), and the frozen script (`docs/F11A_SCENARIO_SCRIPT_2026-07-04.md`) are all done. Implemented on `naavi-voice-server` `staging` branch (commits `dcb9211`..`f2e6afe`): 5 new scenarios (unified_search/geofencing/automatic_alerts/scheduling/lists) replacing today/bills/history/location/capture, plus 3 live-call bugs found and fixed during staging testing — missing pause before gate questions, a stacked-pause bug (two separate `<break>` tags in two different files compounding to 1.8s), and a `speechTimeout=3` outlier on the closer `<Gather>` that was the ACTUAL cause of the "pause too long" reports (not the `<break>` tags at all — see `docs/SESSION_HANDOFF_2026-07-05_F11A_STAGING_F12_PHASE1.md` for the full misdiagnosis trace). Also fixed: the reminder-time parser dropped the hour when Twilio transcribed a spoken time without a colon ("four ten am" → read as "10am"). **NOT DONE YET: the field test itself** — the plan (`F11A_SCENARIO_SCRIPT` §12) calls for 20-30 real calls to the staging number (+1 873-446-2284) tracking interrupt points, strongest-reaction scenario, 3-scenario completion rate, and reminder-conversion rate, before treating this as the new baseline or promoting to production. None of that data has been collected — testing so far was ad hoc bug-hunting, not the structured field test. **Original context below, kept for the capability-gap rationale:** Wael tested MyNaavi zero-context and asked Naavi directly "what are your top 5 capabilities" — got a clear, strong answer: (1) Geofencing/location alerts, (2) Retrieval across systems (contacts+calendar+email+notes searched at once, e.g. "ask about Dr. Smith"), (3) Automation/proactive alerts (email-triggered, weather-triggered, time-based — e.g. "alert when Sarah emails", "text you if it rains tomorrow"), (4) Calendar/scheduling (creating events, medication scheduling), (5) Lists/tasks (create, add/remove, list-to-alert connection). Compared against the current 1-888 demo line's 5 scenarios (today/bills/history/location/capture): only "Location" is a clean match. "Bills" and "History" are narrower, single-domain, read-only lookups — weaker than the actual cross-system retrieval capability (#2). "Today" only reads the calendar back, never demonstrates Naavi creating an event or scheduling medication (#4). **Proactive automation (#3) and Lists (#5) are not represented in the demo at all.** Conclusion: the demo leans on read-only retrieval and undersells two of Naavi's five strongest capabilities entirely, and shows two others in a weaker form than Naavi's own best pitch. **Scope:** rewrite the demo's scenario content (likely still 3-5 scenarios, yes/no-gated, same conversational walkthrough mechanism as the current F2b build) around these 5 validated capabilities instead of the current ad-hoc set. Comparable in scope to the original F2b scenario walkthrough build (script writing, live-call testing, governance review) — not a quick content tweak. **Design policy (Wael 2026-07-04):** going forward, demo/marketing content claiming to showcase Naavi's capabilities should be validated by asking Naavi itself what its top capabilities are, not just written from assumption — see [[project_naavi_demo_capability_alignment_policy]] memory. **Governance (2026-07-04):** unlike F10a, this is NOT waived from review — full Phase 1-8 process per `docs/AI_DEVELOPMENT_GOVERNANCE.md` applies, including ChatGPT technical review before coding (Phase 3) and after coding (Phase 6), same discipline as the original F2b build. This is a content/behavior rewrite of a live public phone line, not a low-risk isolated change. **Input for Phase 1 (2026-07-04):** the capability comparison above (Naavi's 5 self-described capabilities vs. the demo's 5 scenarios, with the specific gaps identified) is the raw material for next session's Phase 1 Problem Definition — not Phase 1 itself. Phase 1 still needs to be written formally (what's broken / evidence / root cause / alternatives considered) before any code, reusing this analysis rather than re-deriving it from scratch. | Server                                        | approved — next session's main priority                                       |
| F12  | Third-party alerts with an unresolved (literal or named-contact) destination could silently misfire to the user themselves instead of the intended recipient — full fix implemented, not yet deployed (2026-07-06) | both    | **Full detail:** `docs/F12_PHASE1_PROBLEM_DEFINITION_2026-07-05.md` (two defects, evidence, architectural precedent), `docs/F12_PHASE2_CHANGE_PLAN_2026-07-05.md` (Recipient Resolver design, approved through 2 external review rounds), `docs/F12_PHASE4_EVIDENCE_2026-07-06.md` (implementation evidence, all 3 tiers). **Root cause (proven live on production):** literal/named destinations never resolved to `to_email`/`to_phone`; `evaluate-rules` then treated the rule as `noRecipient` → self-alert, firing to the user instead of the third party. A separate memory-hit merge-check gap also silently dropped destination changes on repeat visits to the same place. **Fix, all 3 tiers implemented and tested (18/18 F12 tests, 377/377 full suite green):** (1) Defect B merge-check fix, mobile+voice+`manage-rules` — committed, deployed to staging. (2) New `resolve-recipient` Edge Function (the shared Recipient Resolver) + `lookup-contact` `contact_id` support — committed, deployed to staging, built standalone/unwired first per Wael's "zero-risk" instruction. (3) **Not yet committed/deployed:** mobile + voice wiring to `resolve-recipient`, and the `evaluate-rules` Protected Core fire-time re-resolution (contacts are a live reference per Wael's explicit lifecycle decision — re-resolved fresh every fire, not frozen at creation) with a distinct self-notify failure path for an unresolvable contact. **Governance: full Phase 1-8, not waived** — Phase 3 (external review) satisfied via the multi-round review conducted throughout Phase 1/2; Phase 4 (implementation) complete for all 3 tiers; Phase 5 (Evidence Package) written. **Status:** all 3 tiers committed and deployed to staging (main `b034e10`, voice-server `5ada02b`). Production untouched. Next: manual staging validation, then production promotion requires Wael's separate explicit approval.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Server (likely both AAB + Server once scoped) | All 3 tiers deployed to staging — pending manual validation before production |
| F13  | Restore `og:image`/`twitter:image` on mynaavi-website with a proper square share image (2026-07-06)                                                                                                                | website | **Context:** `og:image` was removed site-wide in `a1ea0a6` (2026-07-02) because it pointed at the 512×512 square app icon, which rendered as an oversized image dominating iMessage/RCS threads whenever a mynaavi.com link was texted (found via the F2b demo-line SMS, which includes a `/start` link). During a full SEO audit (2026-07-06) the tradeoff was revisited: missing `og:image` has zero direct Google ranking impact, but does reduce click-through on social/messaging shares (no preview image) and Discover CTR for blog posts (~45% lower per Discover's own image-size guidance). **Decision (Wael 2026-07-06):** keep the share image square (matches the existing brand mark) rather than switch to the standard 1200×630 landscape format — accepting the iMessage square-crop risk documented above, with the explicit fallback plan **"if the problem reappears with SMS we will remove it."** **Blocker:** no 1200×1200 (or any higher-resolution) source asset exists anywhere in the project — checked `mynaavi-website/` root, `web/public/`, and `assets/`; every copy of the logo (icon-512.png, icon-512-Black.png, mynaavi-logo(-transparent).png) tops out at 512×512. Proposed path (not yet executed — Wael said leave as-is, log here instead): upscale the transparent 512×512 mark (`assets/mynaavi-logo-transparent.png`) to 1200×1200 via high-quality resampling, composite onto the site's cream `--bg-app` (`#FAFAF7`) background rather than leave transparent, then wire into `og:image`/`twitter:image` across all live pages.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Server                                        | deferred — Wael: "leave it as is, create it in the Holding list"              |
| F9a  | Google App Actions spike — expanded scope: 3 list built-in intents (2026-07-03)                                                                                                                                    | mobile  | **Scope expanded 2026-07-03 (Wael, documentation only — no build this session).** Original spike covered only `actions.intent.UPDATE_ITEM_LIST` ("Hey Google, ask Naavi to add milk to my Costco list"). Wael's underlying motivation: many users treat Google Keep as their main grocery/todo list; F9a exists to remove the two-step friction (call Naavi, then speak the add command) for getting items into Naavi's list system by voice. **Hard requirement (Wael): voice-only, end to end — no "open the app and confirm by touch" fallback is acceptable for anything in this spike's scope.** Investigated against Google's actual current App Actions documentation (not assumed) — confirmed 3 official built-in intents (BIIs) fit that requirement, all single-utterance/no-confirmation-needed: (1) `UPDATE_ITEM_LIST` — add an item to an existing list (original scope). (2) `GET_ITEM_LIST` — read a list's contents back aloud ("Hey Google, ask what's on my Naavi Costco list") — added at Wael's explicit request 2026-07-03. (3) `CREATE_ITEM_LIST` — start a brand-new list ("Hey Google, ask Naavi to create a camping list") — added because it directly serves the Google Keep bridge goal (a user may want to start a new list before adding to it). **Explicitly ruled out:** location-alert creation by voice ("add an alert when I arrive at X") — Naavi's existing verified-address rule (`project_naavi_location_verified_address` memory) requires an in-conversation confirmation step before any location alert is created, which a single Assistant utterance structurally cannot provide (no multi-turn dialog in Google's App Actions fulfillment model since Conversational Actions was discontinued in 2023); with touch-confirmation fallback also ruled out by Wael, there is no safe voice-only path for this, so it stays out of scope. Attach-list-to-alert and remove-item-from-list were considered but not requested for this expansion — both are technically buildable later via a Google "custom intent" (phrase-matching against known list/alert names) rather than an official BII, since Google has no built-in intent for either. **Architecture (unchanged from original spike):** Expo config plugin generates shortcuts.xml + AndroidManifest patch at prebuild; `lib/app-actions.ts` generic dispatcher (`handleAppAction()` → switch); `parseAppActionURL()` helper (no `new URL()`); URL scheme `naavi://app-actions/v1/list/...`; validation rejects malformed intents. Natural voice invocation requires Play Console upload + Google App Actions review — spike uses Android Studio test tool only. Android phone only (Google Home speakers not supported — hard Google limit). **Branch:** `feature/app-actions-spike` from tag `v300` (still just a branch marker as of 2026-07-03, no spike code written yet). **Spike success criteria (all required, now per-BII):** app-killed cold-start pass, app-backgrounded pass, app-foreground pass, each of the 3 BIIs reaches the orchestrator with correct values, existing OAuth/deep links unaffected. **Scope hard limit:** these 3 list BIIs only — no reminders, alerts, email, Siri, Alexa, production review in this spike. **Regression gate:** npm run test:auto + manual OAuth/deep-link smoke tests must pass before merging to main. **Status:** approved 2026-06-29 (original scope), scope expansion documented 2026-07-03 — starts after v300 production AAB is confirmed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | AAB                                           | approved — expanded scope, pending v300 close                                 |
| F16 | Document `naavi-chat`'s two parallel action-generation systems (deterministic Layer 2 classifier vs. Claude tool-use pipeline) — deferred until after F15 closes (2026-07-09) | backend | **Not a bug — a documentation/architecture gap.** F15's investigation (see `docs/F15_PHASE1_PROBLEM_DEFINITION_2026-07-09.md` §4, Evidence B9-B11) discovered that `naavi-chat/index.ts` routes single-action requests through a fully separate, deterministic path — `classifyIntent()` (Haiku, no tools, hand-written JSON prompt) → `buildActionConfirm()` — that completely bypasses the Claude + `NAAVI_TOOLS` tool-use system for multi-action/chat-level requests. Neither F12 nor this investigation's own first three revisions knew this second system existed; substantial diagnostic effort (Hypothesis Validation, raw tool_use logging) was spent instrumenting the wrong pipeline before it was found. **Recommended by external review, not written yet:** a standalone design doc mapping both systems Also recommended, same deferral (2026-07-09): a small standalone **"Self Override Behavioral Contract"** reference doc (input phrasing → expected persisted `action_config` fields → expected dispatcher decision → expected delivery) that both `report-location-event` and `evaluate-rules` implementations reference, so the parity requirement in `docs/F15_PHASE2_CHANGE_PLAN_2026-07-09.md` §1.6 doesn't silently rot if one dispatcher is edited later without the other. — which message shapes route to which, what each one's recipient/contact-resolution mechanism is (Layer 2's several `naavi-chat`-internal `lookup-contact` branches per F15 Evidence B8, vs. Claude+tools' `resolve-recipient` per F12) — so future recipient/action-related debugging starts from an accurate map instead of re-discovering the split the way F15 had to. Explicitly deferred until F15's own Phase 4-8 close out, not blocking. | Server | deferred — logged only, not started |
| F15 | Recipient EXTRACTION dropped in two patterns: self-alert with an explicit literal destination override, and third-party named-contact recipients on location alerts (2026-07-09) | mobile | **F15 CLOSED (2026-07-09).** Both defects (Defect B — recipient dropped on location alerts; Defect A — self-alert destination override) fully implemented, deployed to staging, and live-validated end to end — write path and fire path for both, including real SMS/email/voice delivery confirmed via direct testing. A third defect was found and fixed mid-validation: Claude choosing `draft_message` instead of `set_action_rule` for a self-addressed override phrasing, root-caused to an overly broad early prompt rule in `get-naavi-prompt/index.ts`, fixed with an explicit exclusion. Evidence: `docs/F15_PHASE5_EVIDENCE_DEFECT_A_2026-07-09.md`, `docs/F15_PHASE5_EVIDENCE_DEFECT_B_2026-07-09.md`, design trail in `docs/F15_PHASE1_PROBLEM_DEFINITION_2026-07-09.md` and `docs/F15_PHASE2_CHANGE_PLAN_2026-07-09.md`. **Remaining work is explicitly non-blocking for F15 itself:** client-side display polish (`app/alerts.tsx`) code-complete, batched for a future dedicated APK session, not built standalone (B9e); production promotion requires Wael's separate explicit approval per the staging-first rule. Six related, separate items spun out and logged independently, none blocking: B9a (ambiguous verbs default silently), B9b (phone/email query mismatch), B9c (disabled list still shows active), B9d (safe-area insets race + possible force-stop/geofence link), B9f (evaluate-rules callVoice not logged to sent_messages), F16 (deferred two-engine architecture doc). | Server (mobile-only per Phase 1 scope boundary) | CLOSED |
| F17 | Voice has no equivalent of F15 Defect A's self-override fix — structurally blocked, not just untested (2026-07-09) | voice | **Confirmed by direct code inspection, not inferred.** Voice runs its own independently-maintained tool schema at `naavi-voice-server/src/anthropic_tools.js` (separate repo from mobile's `supabase/functions/_shared/anthropic_tools.ts`, which F15 Defect A updated). That file's `ACTION_CONFIG` (lines 88-104) has `additionalProperties: false` and declares no `self_override_*` fields at all -- Claude is structurally prevented from emitting them on a voice call, not merely unlikely to. Grepped the entire `naavi-voice-server/src/index.js` (~11,000 lines) for `self_override` -- zero matches; the `SET_ACTION_RULE` handler (line 4669 onward) has no awareness of the fields. Voice also has no staging/production split (single shared Twilio account + single Railway deployment, confirmed same session) -- so this cannot be fixed or tested incrementally the way mobile was; any voice fix ships straight to the only voice environment that exists. **Predicted (not yet live-tested) behavior today:** "text me at [number] when I arrive at [address]" on a phone call puts the number in `to` (the only field voice's schema has), which resolves as a third-party recipient via `resolve-recipient` -- the alert fires third-party-style (single channel, no fan-out, no per-channel preferences) instead of as a self-alert with a channel override. This is F15 Defect A's original bug, unfixed on this surface. Explicitly out of scope for F15 per `docs/F15_PHASE2_CHANGE_PLAN_2026-07-09.md` Section 4 item 7 ("Voice-side F15 investigation -- separate Phase 1, not started, not implied by this plan") -- this entry makes that follow-up concrete with exact file/line evidence instead of a placeholder. Wael's plan (2026-07-09): (1) test mobile live on staging, (2) promote to production AAB, (3) implement voice as its own dedicated fix after. Needs its own Phase 1 problem definition before coding -- mirrors mobile's four self_override_* fields (`self_override_email`/`self_override_sms`/`self_override_whatsapp`/`self_override_voice`) but the write path, schema, and downstream forwarding are all voice-specific and unverified. | Server (naavi-voice-server, separate repo/deploy from mobile and from Supabase Edge Functions) | open -- not yet started |

---

## Tooling (T) — OPEN

| ID | Description | Surface | Notes | Server/AAB | Status |
|----|-------------|---------|-------|------------|--------|

---

---

## Ideas (I) — Deferred

| ID | Description | Surface | Notes | Server/AAB | Status |
|----|-------------|---------|-------|------------|--------|
| I4a | Google Play in-app star-rating prompt (2026-07-03) | mobile | **Suggested by Claude, not yet decided — for discussion, not committed to build.** Raised while scoping F10a's feedback expansion; Wael asked to log it separately as an alternative to weigh rather than build now. Verified against Google's official Android Developers in-app-review documentation (not assumed): Google explicitly prohibits a button/CTA that triggers their native review dialog, since the dialog is quota-limited (undocumented, roughly once per some period) and a button that silently does nothing when quota is hit looks broken. Proposed design has two separate, complementary pieces: (1) **Automatic smart prompt** — `expo-store-review` (compatible with the app's current Expo SDK 55), triggered silently in the background at a good usage moment (e.g. after several successful sessions over a few days), never from a button, never paired with a pre-question ("enjoying the app?"), tracked with our own throttling (last-shown timestamp + usage count in local storage) since Google's quota is opaque and can change without notice. (2) **Manual "Rate MyNaavi" link** (e.g. in Settings or Help) — NOT the same mechanism, just a plain link to the app's actual Google Play Store listing page; unrestricted, always works, gives users who want to rate anytime a guaranteed path even when the automatic prompt's quota is exhausted. | AAB | idea — not yet decided, for discussion |
| I2a | `list_change` alert trigger | backend | Alert when a list changes — e.g., *"alert when grocery list hits 10 items"* or *"alert when to-do is empty."* Deferred — 7 design questions open with stub answers (third-party routing, threshold semantics, etc.). ~½ session design + ½ session build. | Server | deferred (design questions open) |
| I2b | `price` alert trigger | backend | Alert when a price drops — flight, retail item, gas. Deferred — external integration path not chosen (scraping fragility, paid-API costs, vertical fragmentation across flights / hotels / retail / gas). Path-selection decision is a focused session; build is real engineering after that. | Both | deferred (path-selection open) |
| I3a | `health` alert trigger (Epic / wearable integration) | backend | Alert when a health metric changes — *"alert me if my pulse is above 120"*, *"text my wife if BP > 180"*. Blocked — Epic FHIR account, healthcare-data agreement, privacy review, and wearable SDK integration are multi-month wall-clock prereqs. | Both | blocked (multi-month prereqs) |

---

---

# CLOSED ITEMS

All items confirmed done. Moved here to keep active tables clean.

---

## Closed Bugs (B)

| ID  | Description                                                                                                                                  | Surface       | Closed     | Reason                                                                                                                                                                                                                                                                                                                                                          |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B9i | Self-override WhatsApp time-trigger alert showed a clean "Alert set." confirmation while nothing was written to `action_rules` | mobile/server | 2026-07-13 | Root cause: `naavi-chat`'s time-trigger confirm-then-act path never embedded a `PENDING_INTENT` marker for self-override requests (only built for named third parties), so the "yes" turn had nothing for Step 1.4 to deterministically execute — Claude just emitted its trust-the-server acknowledgment text with no tool call. Fixed entirely server-side in `naavi-chat/index.ts` (no APK needed): self-override case now builds the confirm speech and embeds the marker synchronously. Verified via direct `action_rules`/`sent_messages` DB queries and live retest — Wael confirmed "Passed." Regression test: `tests/catalogue/session-2026-07-10-b9i-self-override-time.ts`. **Promoted to production 2026-07-13** (`naavi-chat` deployed to `hhgyppbxgmjrwdpdubcx`). |
| B9j | WhatsApp self-override alerts were accepted by Twilio but never delivered to the phone | server/infra | 2026-07-13 | Staging was on Twilio's WhatsApp Sandbox (error 63015, recipient never joined it). Connected an approved production WhatsApp sender (`+12494211584`) via Meta's WhatsApp Manager, added to the existing WABA. Retest then hit a second failure (error 63016, WhatsApp's 24h session-window rule) — fixed by setting `TWILIO_WHATSAPP_TEMPLATE_MESSAGE_SID` to an already-approved template (`send-sms/index.ts` had full template support built in since 2026-04-09, just never wired up on staging). No application code changed — both were missing/wrong staging secrets. Verified delivered on-device. **Promoted to production 2026-07-13 — critical finding along the way: production's `TWILIO_WHATSAPP_FROM` secret was still set to `+16138796681` (the number disconnected from Meta's WhatsApp Manager during this same session), meaning production WhatsApp sending was silently broken from the moment the old number was disconnected. Confirmed live via a temporary read-only diagnostic function before changing anything. Fixed: `TWILIO_WHATSAPP_FROM` updated to `+12494211584` on production; `TWILIO_WHATSAPP_TEMPLATE_MESSAGE_SID` on production already correctly pointed at the same approved template (confirmed, not assumed).** |
| B9k | Self-alert WhatsApp messages read like a third party named "Naavi" messaged the user ("Hi Robert, Naavi shared this message with you...") | server | 2026-07-13 | The approved template's fixed wording is correct for third-party alerts but confusing for self-alerts. Fixed in `evaluate-rules/index.ts`: the self-alert branch now overrides the template variables to a note-to-self reading ("Hi there, Robert shared this message with you...") using the same Meta-approved template — no resubmission needed. Third-party alerts unaffected. Verified — Wael confirmed "Passed." Regression test: `tests/catalogue/session-2026-07-13-b9k-self-alert-whatsapp-wording.ts`. **Promoted to production 2026-07-13** (`evaluate-rules` deployed to `hhgyppbxgmjrwdpdubcx`). |
| B9l | Classifier (Haiku) inconsistently put a self-override phone number into `to_name` instead of `self_override_whatsapp`, misrouting the request into a "couldn't find a phone number in your contacts" error | server | 2026-07-13 | Found live: identical phrasing that worked 3 prior times classified differently on a 4th attempt. Fixed with a deterministic code guard in `naavi-chat/index.ts` — not a prompt tweak, per this codebase's never-trust-the-LLM-to-be-consistent design philosophy: a phone-shaped `to_name` with no `self_override_*` already present is reclassified into the matching field before the contact-lookup path runs. Verified — Wael confirmed "Passed." Regression test: `tests/catalogue/session-2026-07-13-b9l-phone-shaped-to-name.ts`. **Promoted to production 2026-07-13** (same `naavi-chat` deploy as B9i). |
| B8b | Staff-created "Phone call" tickets wrongly default to SMS reply | staff portal | 2026-07-03 | **Separate from F8b** (F8b = missing feature: staff manually choosing reply channel; this was a correctness bug in the existing automatic default). Root cause: `ingest-ticket` only persisted `created_by` for the `internal-relay` channel, discarding it for `voice-call`/`web-contact`/`mobile-contact` — so a ticket a staffer manually logged after receiving a call was indistinguishable from one the live voice system created automatically, and both got a supplementary SMS reply. Note: email always sends via Postmark regardless of channel — the SMS is a supplementary send, not a replacement, so the actual bug was staff-created tickets getting an *unwanted extra SMS*, not the wrong channel outright. Fix: migration `20260703000001_tickets_created_by.sql` adds `tickets.created_by`; `ingest-ticket` persists it for every channel and skips the ticket-received SMS when set; `send-ticket-reply` skips the supplementary SMS reply when set. Staff portal's "How did they contact us?" dropdown is unchanged — kept for auditing/tracking (Wael: "no harm") but no longer drives reply-channel logic. Applied to staging + production; verified `created_by` persists via direct test; 3 new regression tests in `tests/catalogue/session-2026-07-02.ts`; 354/356 auto-tester green.                                                                                                                                               |
| B4v | Naavi rejects user location pick with "closer to you" — intermittent                                                                         | mobile/server | 2026-06-11 | Cannot be reproduced. Confirmed 2026-05-23 and again 2026-06-11. The phrase is not hardcoded — was Claude generating it editorially. No recurrence since. Reopen if it surfaces again with a captured transcript.                                                                                                                                               |
| B2a | Voice promises to schedule medication but doesn't create the events                                                                          | voice         | 2026-05-19 | Confirmed fixed in prior session; voice now creates calendar events for medication scheduling.                                                                                                                                                                                                                                                                  |
| B2b | You can't interrupt Naavi mid-sentence on the phone                                                                                          | voice         | 2026-05-09 | `stopMusic()` now drains Twilio's outbound buffer via `event: 'clear'`.                                                                                                                                                                                                                                                                                         |
| B2c | You can't talk over Naavi on the phone                                                                                                       | voice         | 2026-05-09 | Same fix as B2b.                                                                                                                                                                                                                                                                                                                                                |
| B2d | Voice name-search mistranscription ("Hussein")                                                                                               | voice         | 2026-05-08 | F2c approach abandoned; keyterms mitigation remains. Reopen if recurs.                                                                                                                                                                                                                                                                                          |
| B2e | Naavi misses recent emails until hourly sync                                                                                                 | server        | 2026-05-10 | Live-overlay window widened to 24h + capacity raised 10→30 emails.                                                                                                                                                                                                                                                                                              |
| B2g | Voice live-calendar fetch — voice surface on stale snapshot                                                                                  | voice         | 2026-05-19 | Confirmed fixed in prior session. Voice now reads the live calendar overlay.                                                                                                                                                                                                                                                                                    |
| B2h | Voice "Naavi stop" interrupt regression mid-TTS                                                                                              | voice         | 2026-05-19 | `stopMusic()` drain commits fixed the interrupt path.                                                                                                                                                                                                                                                                                                           |
| B2i | Voice Deepgram drops leading word during barge-in                                                                                            | voice         | 2026-05-19 | Confirmed fixed in prior session.                                                                                                                                                                                                                                                                                                                               |
| B2j | Voice name-search STT mistranscription                                                                                                       | voice         | 2026-05-19 | Confirmed fixed; keyterms-capture + silence-detection improvements covered residual cases.                                                                                                                                                                                                                                                                      |
| B2l | Orphan SDK geofence — deleted action_rule still fires T1 events on user's phone                                                              | mobile        | 2026-05-23 | `syncGeofencesForUser` called from all 4 delete paths. Live in V57.20.1 build 194.                                                                                                                                                                                                                                                                              |
| B3a | User hears two voices on mobile: Naavi's voice + phone's built-in voice                                                                      | mobile        | parked     | Build 166 shipped Path 1. Path 2 parked until cloud-voice consistency becomes a recurring complaint.                                                                                                                                                                                                                                                            |
| B3b | Cosmetic ruler leak on long-wrap user bubbles                                                                                                | mobile        | 2026-05-08 | Build 166 `color: 'transparent'` → `opacity: 0`. Not retested by Wael — cosmetic only.                                                                                                                                                                                                                                                                          |
| B3c | Haptic vibration feels too subtle on Samsung long-press                                                                                      | mobile        | parked     | Samsung Android 14 haptic API issue. Reopen with new approach when haptic UX is priority.                                                                                                                                                                                                                                                                       |
| B3d | Verified-address rejection doesn't name the address                                                                                          | mobile        | 2026-05-15 | Bundle `bd52106` + polish `5ce56ad`. Mobile + voice paths covered.                                                                                                                                                                                                                                                                                              |
| B3e | Two blog articles still on age framing                                                                                                       | website       | 2026-05-19 | Reclassified as not-a-bug — blogs stay with age framing per 2026-05-18 decision.                                                                                                                                                                                                                                                                                |
| B3f | `resolve-place` radius 100→500 + numbered-address routing                                                                                    | server        | 2026-05-19 | Default radius now 300m. Bumped from 100m in V57.16.                                                                                                                                                                                                                                                                                                            |
| B3g | OAuth silent-revoke detection                                                                                                                | server        | 2026-05-23 | Deleted — pattern did not recur. Reopen if second user hits silent-revoke.                                                                                                                                                                                                                                                                                      |
| B3h | `isValidE164` strict 10-digit-after-+1 enforcement                                                                                           | server        | 2026-05-19 | Confirmed fixed in prior session.                                                                                                                                                                                                                                                                                                                               |
| B3i | Brief reader — all-day events invisible to assistant-fulfillment + person-event lookup                                                       | server        | 2026-05-19 | Two-query pattern applied to `assistant-fulfillment` + `lib/memory.ts`.                                                                                                                                                                                                                                                                                         |
| B3z | OAuth refresh_token rotation not written back to user_tokens                                                                                 | mobile        | 2026-06-10 | Removed `naavi_google_oauth_pending` gate at `lib/calendar.ts:154`. Fix committed; ships with next AAB.                                                                                                                                                                                                                                                         |
| B4a | Stop-word interrupt regression — "Naavi stop" no longer cuts TTS                                                                             | voice         | 2026-05-23 | Doesn't reproduce — barge-in catch-all fires before stop-words matcher.                                                                                                                                                                                                                                                                                         |
| B4b | Deepgram first-word truncation on barge-in                                                                                                   | voice         | 2026-05-23 | Trivial-fast-path regex covers name-truncation variant.                                                                                                                                                                                                                                                                                                         |
| B4c | Voice name-search mistranscription (Hussein, etc.)                                                                                           | voice         | 2026-05-23 | Spell-out fallback `processSpelling` verified shipped and live.                                                                                                                                                                                                                                                                                                 |
| B4d | SCHEDULE_MEDICATION action missing on voice                                                                                                  | voice         | 2026-05-23 | Duplicate of B2a. Tool defined in `anthropic_tools.js:349`, handler at `index.js:3101`.                                                                                                                                                                                                                                                                         |
| B4e | DELETE_MEMORY voice vs mobile behavior diff                                                                                                  | voice         | 2026-05-23 | No drift confirmed; entry was stale. Both surfaces trim identically.                                                                                                                                                                                                                                                                                            |
| B4f | Mobile TTS reads addresses wrong — postal codes + street abbreviations + province codes                                                      | mobile        | 2026-06-10 | Fix committed (`sanitiseForSpeech` normalization before char-splitter); ships with next AAB.                                                                                                                                                                                                                                                                    |
| B4g | Province codes on mobile TTS                                                                                                                 | mobile        | 2026-05-23 | Merged into B4f.                                                                                                                                                                                                                                                                                                                                                |
| B4h | Ordinal expansion on voice TTS                                                                                                               | voice         | 2026-05-23 | Merged into B4i.                                                                                                                                                                                                                                                                                                                                                |
| B4i | Address read-back trust bar on voice (3 pieces)                                                                                              | voice         | 2026-05-23 | 2 of 3 pieces shipped on voice server (`normalizeAbbrevForTTS`). Third piece (ordinal) deferred as low-impact.                                                                                                                                                                                                                                                  |
| B4j | Rule with `list_name` reference doesn't create/connect the list                                                                              | both          | 2026-05-23 | `ensureListAttachedToRule` helper shipped mobile + voice. Live V57.22.2 build 198.                                                                                                                                                                                                                                                                              |
| B4k | HubSpot Reply Send shows cosmetic "automation error" banner                                                                                  | server        | 2026-06-10 | HubSpot retired from Naavi.                                                                                                                                                                                                                                                                                                                                     |
| B4l | Auto-re-prompt for location permission on every app foreground                                                                               | mobile        | 2026-05-23 | Fix shipped V57.21.0 build 195. `hooks/useGeofencing.ts:617-666`.                                                                                                                                                                                                                                                                                               |
| B4m | Mutex around `BackgroundGeolocation.start()` to eliminate concurrent-start throws                                                            | mobile        | 2026-05-23 | Mutex shipped V57.21.0 build 195. `_startGeofencesPromise` pattern at `hooks/useGeofencing.ts:544-561`.                                                                                                                                                                                                                                                         |
| B4n | Permission-status banner on Alerts screen                                                                                                    | mobile        | 2026-05-23 | Banner shipped V57.21.0 build 195. `app/alerts.tsx` lines 610-624.                                                                                                                                                                                                                                                                                              |
| B4o | Server-side daily geofence health-check cron                                                                                                 | server        | 2026-05-23 | `geofence-health-check` Edge Function live. Daily cron at `0 12 * * *` UTC (job ID 22).                                                                                                                                                                                                                                                                         |
| B4p | TRUST BREACH — Naavi says "Added" while silently skipping LIST_ADD on short/test-shaped items                                                | server        | 2026-05-23 | RULE 21 added to `get-naavi-prompt`. Both regression tests green.                                                                                                                                                                                                                                                                                               |
| B4q | All-day events invisible to voice brief AND mobile chat live overlay                                                                         | both          | 2026-05-23 | Two-query Promise.all (timed + all-day) on both voice and mobile chat paths.                                                                                                                                                                                                                                                                                    |
| B4r | Mobile TTS reads bulleted lists as one continuous sentence                                                                                   | both          | 2026-05-23 | Two-field architecture (speech + display) live in V57.22.2 build 198.                                                                                                                                                                                                                                                                                           |
| B4s | Voice-server entity-existence parity                                                                                                         | voice         | 2026-05-28 | Code audit confirmed all missing pieces already ported. No code change needed.                                                                                                                                                                                                                                                                                  |
| B4t | HubSpot auto-acknowledgment email never reaches gmail.com recipients                                                                         | server        | 2026-05-23 | Verified working — symptom did not reproduce.                                                                                                                                                                                                                                                                                                                   |
| B4u | Location-clarification UX exposes attempt counter ("1 try left")                                                                             | mobile        | 2026-05-23 | Counter removed from `hooks/useOrchestrator.ts:1104-1109`. Ships in next AAB.                                                                                                                                                                                                                                                                                   |
| B4w | Postal-code search (no-space format) missed contacts stored with space                                                                       | server        | 2026-06-11 | Root cause: anchor-term filter in `global-search/index.ts` compared anchor word "k1c5m3" against snippet "k1c 5m3" — space mismatch caused match to fail. Fix: `anchorMatch` now also checks space-stripped `hayNorm` against space-stripped anchor word. 243/243 tests green.                                                                                  |
| B4x | TRUST BREACH — Naavi denies existence of disabled alerts visible on Alerts screen                                                            | server        | 2026-05-24 | Alerts context now injects ACTIVE + DISABLED sections. Auto-reactivate preprocessor live.                                                                                                                                                                                                                                                                       |
| B4y | TRUST BREACH — Naavi creates unauthorized email-alert rules from search-shape utterances                                                     | both          | 2026-06-10 | HAS_CREATE_INTENT gate shipped. Remaining confirm gap → B6i.                                                                                                                                                                                                                                                                                                    |
| B2m | Parity audit — mobile vs voice gaps                                                                                                          | both          | 2026-06-15 | All 7 gaps resolved or accepted: (1) Re-arm expired location alert ✅; (2) SPEND_SUMMARY ✅ already existed; (3) LOG_CONCERN/UPDATE_PROFILE ✅; (4) UPDATE_MORNING_CALL brief_windows ✅ both surfaces + timeToWindow zero-pad fix (build 255); (5-7) Low gaps accepted-no-action. See parity audit doc.                                                            |
| B6a | Re-arm expired location alert — mobile orchestrator + voice server                                                                           | both          | 2026-06-11 | Mobile: fix committed `318e522`; ships with next AAB. Voice: `pendingRearm` state added to voice server — substring place-name match, in-call re-arm offer on "yes", note/body merged from new request. Tested on voice by Wael 2026-06-11. Both surfaces closed.                                                                                               |
| B6g | Voice location alert created via picker lands without coordinates — geofence never arms                                                      | voice         | 2026-06-11 | Root cause: voice server bypassed `resolve-place` for expired/existing alerts, creating a duplicate rule without lat/lng. Fix: pre-resolve memory-hit check (B6a) intercepts named places before the picker is shown — expired alert is re-armed in-call; enabled alert with different note offers note update. Picker path for genuinely new places unchanged. |
| B6b | One-time cleanup migration: collapse duplicate disabled location rules                                                                       | backend       | 2026-05-26 | Migration `20260526_action_rules_one_row_per_place.sql` applied. Tests green.                                                                                                                                                                                                                                                                                   |
| B6c | Keyboard flicker / jump on chat input                                                                                                        | mobile        | 2026-06-10 | `app.json` `"pan"` → `"resize"` + KAV disabled on Android. Fix committed; ships with next AAB.                                                                                                                                                                                                                                                                  |
| B6d | Choice/option lists rendered as bullets instead of numbered                                                                                  | server        | 2026-06-10 | Multiple live tests passed. Reopen if regression observed.                                                                                                                                                                                                                                                                                                      |
| B6e | Calendar query misroutes to LIST_READ                                                                                                        | server        | 2026-05-28 | `isCalendarReadIntent()` bypass gate in `naavi-chat`. Regression tests green.                                                                                                                                                                                                                                                                                   |
| B6f | Supabase migration tracking misaligned with prod state                                                                                       | backend       | 2026-06-10 | All 52 migration files renamed to 14-digit format. CLI push now works cleanly.                                                                                                                                                                                                                                                                                  |
| B6h | ⭐ TRUST BREACH — DELETE_RULE says "Done" but alert is not deleted                                                                            | mobile        | 2026-06-11 | Pre-Claude delete-intent intercept added to `hooks/useOrchestrator.ts`. 0 matches → tells user. 1 match → confirm-before-delete (pendingConfirmDeleteRef). 2+ matches → disambiguation (pendingDeleteRef). Confirm handler checks `data.ok === true` before saying "Done". Tested on mobile + voice by Wael 2026-06-11. Ships with next AAB.                    |
| B6i | Naavi executes state-changing actions without asking the user to confirm first                                                               | both          | 2026-06-11 | Tested by Wael 2026-06-11 — does not reproduce. Confirm-then-act flow already in place for tested actions. Reopen if specific action found that executes without confirmation.                                                                                                                                                                                  |
| B7a | Duplicate `client_diagnostics` log events — every DB log event fires in pairs                                                                | mobile        | 2026-06-11 | Root cause: `app/index.tsx` AppState listener called `remoteLog` on every state change; `appLifecycle.ts` already does the same. Duplicate listener removed from `app/index.tsx`. Ships with next AAB.                                                                                                                                                          |
| B7c | Homepage storyboard iframes not running                                                                                                      | website       | 2026-06-11 | Tested by Wael 2026-06-11 — iframes working correctly. No fix required.                                                                                                                                                                                                                                                                                         |
| B7b | Voice bare-name transcription loss — Deepgram drops leading verb, falls through to hallucination                                             | voice         | 2026-06-11 | Tested by Wael 2026-06-11 — does not reproduce. Phone-operator confirmation flow already handles bare-name input correctly. Reopen if specific hallucination observed.                                                                                                                                                                                          |
| B7d | SET_REMINDER handler in useOrchestrator — retire `saveReminder()` + calendar event creation; replace with action_rules INSERT (time trigger) | mobile        | 2026-06-15 | RULE 3 prompt already updated (v116) to route "remind me at X" → set_action_rule(trigger_type='time'). This is the mobile-side cleanup: remove `saveReminder()` call at `hooks/useOrchestrator.ts:2699`, remove the calendar event block at lines 2701-2714, and insert directly into `action_rules` instead. Ships with next AAB.                              |

---

## Closed Features (F)

| ID | Description | Surface | Closed | Reason |
|----|-------------|---------|--------|--------|
| F14 | Precompile mynaavi-website demo storyboard animations instead of live in-browser Babel/React | website | 2026-07-07 | **Shipped to production.** Full governance trail: `docs/F14_PHASE1_PROBLEM_DEFINITION_2026-07-07.md`, `docs/F14_PHASE2_CHANGE_PLAN_2026-07-07.md` (2 external review rounds), `docs/F14_PHASE4_EVIDENCE_2026-07-07.md` (Phase 6 external review approved, Phase 7 testing complete pre- and post-push). Root cause: 5 demo iframes each loaded React/Babel Standalone/Tailwind CDN and compiled JSX live on every page load — confirmed via live Lighthouse test that Babel alone accounted for ~86% of script execution time (8,809ms). Precompiled via Babel CLI + Tailwind CLI (first build tooling this repo has had), output committed rather than built by Vercel (`vercel.json` `framework`/`buildCommand` override, verified against current Vercel docs). Commit `9bae1af` on `origin/main`. **Production result, measured directly:** Performance score 28→75, Total Blocking Time 2,820ms→**0ms**, page weight 1,243→546 KiB. All 5 demos confirmed rendering correctly on the live homepage post-deploy. Build reproducibility confirmed via clean-checkout rebuild (byte-identical output). |
| F10a | Feedback + ticket-submission completeness across website/mobile, pre-Play-Store-launch | both | 2026-07-04 | **Shipped.** Fixed a real bug found during the audit: `app/report.tsx` ("Report a problem") was completely unreachable from any UI and still posted to the retired Formspree endpoint instead of `ingest-ticket` — now fixed (`source_channel: 'mobile-report'`, matching `contact.tsx`'s working pattern) and wired back into `app/help.tsx`'s row list. Added a home-screen feedback banner ("Got feedback? Click here to tell the team") — iterated per Wael's live device testing: initially dismissible with AsyncStorage persistence, corrected to **always visible, no dismiss** (same recurring-reminder pattern as Today's Brief — the point is the user is reminded feedback is welcome every time, not shown it once). Added a homepage-only "Feedback" nav link on `mynaavi-website` (shared.js's buildNav, scoped via `isHome` so it doesn't appear on other pages). Website change reviewed via a real Vercel preview branch before merging to main. 5 new regression tests across `session-2026-07-03-f10a.ts`, all green. Deployed: mobile shipped in production build V1.0.301 (versionCode 301); website merged and live. **Governance note (2026-07-04):** Phase 3/6 ChatGPT review was explicitly waived after Wael asked for a direct risk assessment (Low risk, no Protected Core touched) — not skipped silently; Phases 1, 2, 4, 5, 7, 8 were followed. I4a (Google Play star-rating prompt) remains separate, not yet decided. |
| F2i | 888 toll-free number Twilio Verification (TFV) | voice | 2026-07-03 | **Approved and shipped.** 3rd TFV submission (2026-07-02, corrected Use Case description, Opt-in type Verbal, consent proof at https://mynaavi.com/terms#sms-consent) was approved by Twilio 2026-07-03 -- confirmed directly via Twilio's Tollfree Verification API (status `TWILIO_APPROVED`, channel endpoint assignment linked to +18889162284, sms/mms capabilities enabled). Shipped same day: production's demo-line outbound SMS now sends from the 888 number itself (`demoSmsFrom` in `getDemoEnvironment.js`) instead of the +14313006228 workaround number used while verification was pending. Both demo SMS templates (Recap + Reminder) now also include "Call again: 1-888-91-NAAVI." as readable body text -- note this is body text only, since Twilio's Alphanumeric Sender ID (showing custom text as the "From") is not supported in the US/Canada; the recipient's phone will still show the actual digits as sender. 68/68 voice-server tests green. Staging unaffected (always used its own separate number). Commit `3a537d0` on `naavi-voice-server` `main`. |
| F8d | Compound answer auto-scroll to top | mobile | 2026-06-23 | **Shipped in build V286 (commit f058430), live in production.** When a new turn is a compound result or a planning breakdown (`isCompoundResult` or assistant speech starting with "Here are your"), `app/index.tsx` scrolls the conversation to the top instead of the bottom, so the numbered response list (item #1) is the first thing visible instead of the (often long) user message above it. Also scrolls to top when advancing between items in an active compound session, so each new item is seen first. Shipped slightly after the original "Target V284" note, but done. No dedicated auto-tester regression test exists — mobile-only scroll behavior the server-side test harness can't reach (pre-existing coverage gap from when it shipped, not new). |
| F8c | OAuth scope verification + auto-retry on missing scopes | mobile | 2026-06-19 | **Shipped in build 267, live in production.** After every SIGNED_IN event, `checkGrantedScopes()` (`lib/supabase.ts`) silently probes all 4 Google APIs (Calendar, Gmail, Contacts, Drive) using `session.provider_token`; 401/403 = missing scope, network errors are swallowed so sign-in is never blocked. If any scope failed, `app/index.tsx` shows a one-tap "One more step" modal — tapping OK re-triggers `signInWithGoogle()` automatically, popping the Google consent screen again with no diagnosis needed from the customer. Verified happy-path on 2 phones (all scopes already granted — no modal shown); the modal-trigger edge case requires revoking Google account access manually and was not tested on a production account, per `docs/SESSION_HANDOFF_2026-06-19_F8C_BUILD267.md`. 2 regression tests in `tests/catalogue/session-2026-06-19.ts` (`f8c.check-granted-scopes-detects-failures`, `f8c.scope-probe-hooked-into-signed-in`), both green. |
| F8b | Support ticket SMS flow — staff portal channel selector | staff portal | 2026-07-03 | **Decided not needed — closed by Wael.** Original gap: staff should be able to manually choose the reply channel (email/SMS) instead of it being auto-determined. Wael's explicit reasoning: the only two channels that ever reach a customer are email or SMS — there is no third option — and the split is already correct without a manual picker: every ticket gets email (required to create any ticket at all), and only a REAL live 1-888 call additionally gets a supplementary SMS (the caller may have just hung up with no email open). B8b (2026-07-03) closed the one gap that made this default wrong: staff-created tickets logged via the "Phone call" dropdown were indistinguishable from real live-call tickets and wrongly got the extra SMS too — now fixed via `tickets.created_by`. With that fixed, the automatic default is correct in all cases, so a manual channel-selector adds no value. The staff portal's "How did they contact us?" dropdown is kept for auditing/tracking only (Wael: "no harm"), not for driving reply-channel logic. |
| F5b | Self-cleansing memory on voice (STT variant dedup) | voice | 2026-06-15, follow-ups + final threshold decision closed 2026-07-02 | **Fully shipped, nothing left open.** Design: embedding-similarity check at write time (not a hash, not Claude-judges-at-read-time). `ingest-note` calls `match_knowledge_for_dedup` (pgvector cosine distance) before every insert; if distance < 0.10 (similarity > 0.90), updates the existing fragment instead of creating a duplicate — catches STT variants like "Hussein"/"Houssain"/"Hoosein". Live in production since 2026-06-15. **2026-07-02 follow-ups, both shipped:** (1) `ingest-note` now returns `dedup_matched`/`dedup_distance` per fragment instead of only logging server-side; (2) shared prompt (`get-naavi-prompt` v132) instructs Claude to say "I already had that noted" instead of a fresh-save confirmation when a fragment's `dedup_matched=true`. Deployed + verified on staging and production. **Also found and fixed 2026-07-02:** staging was missing `OPENAI_API_KEY` entirely, so embeddings — and therefore all of F5b's dedup — silently never ran there; fixed, verified working. **Final decision on the 0.10 threshold (2026-07-02, Wael):** leave it as-is. Real-call testing showed the threshold may be tighter than what Claude's own paraphrasing produces for a repeated fact (measured distance 0.356 for a same-fact, different-spelling test that should have matched) — but a read-only check of real user data (Wael + Huss) found zero actual duplicates today, so there is no active harm to fix. Deliberately not loosening the threshold: doing so would trade a low-frequency, low-harm failure mode (an occasional duplicate memory) for a higher-harm one (a looser threshold is more likely to accidentally merge two *different* people's facts that happen to be phrased similarly, silently overwriting one person's memory with another's). **If this becomes a real problem later:** the recommended fix is not a looser number — have Claude extract a specific "who/what this fragment is about" tag and match on that with spelling-tolerant (e.g. phonetic) comparison, instead of comparing whole-sentence semantic similarity. More precise for the STT-spelling problem this was built for, without the merge-different-people risk. |
| F2b | Demo line — zero-friction stranger experience + conversion path | voice | 2026-07-02 | **Shipped and live in production.** Superseded its own original design (the "spell your name" flow described in the stale OPEN entry never shipped — replaced by a 5-scenario conversational walkthrough before the reminder ask). Built: scenario walkthrough (Today/Bills/History/Location/Capture, yes/no gated, 3-scenario cap), reminder capture (timezone → time → optional message → SMS), Recap SMS (separate from Reminder SMS, environment-aware sender number), verbal STOP opt-out, SMS consent disclosure (for TFV — see F2i). Found and fixed via real call testing: closer intent recognition ("a reminder" alone), false retry-after-decline, ambiguous reminder-time prompt, consent pacing, demo-caller label collision (two callers sharing a name), missing production `DEMO_USER_ID`/`demo_optouts` table (see F2j). Promoted to production 2026-07-02 after full governance review (Phase 3/6 ChatGPT review, staging validation, live-call fixes). `naavi-voice-server` `main` at commit `2803553`. |
| F8a | Support ticket creation from voice phone line | voice | 2026-06-19 | Shipped: "I need help" → 3-turn bypass flow (trigger → description → confirmation → ticket creation). SMS confirmation to caller. `ingest-ticket` suppresses email/SMS for test tickets. 6 regression tests in `session-2026-06-19.ts`. |
| B8a | LIST_RULES synthesize-action backstop | both | 2026-06-17 | Tested by Wael — works on both mobile and voice. No backstop code needed. |
| F4f | Caller PIN for off-phone verification — 4-digit PIN lets Wael call from any phone and be recognized | mobile | 2026-06-17 | Built and live in mobile Settings. Design in `project_naavi_caller_pin_chosen_over_biometric.md`. |
| F2a | Onboarding Review (multi-phone + 7 other gaps) | mobile | 2026-06-16 | Closed by Wael — done. |
| F1a | Lists wired to entities (alerts / calendar events / reminders) | both | 2026-05-15 | Waves 1 / 2 / 2.5 / 2.6 all shipped. Live V57.15.x. |
| F1b | Inbound SMS / WhatsApp queryability | backend | 2026-05-08 | No viable architecture. WhatsApp API is B2C only; SMS via OS-level READ_SMS carries Play rejection risk. |
| F1c | Voice privacy UX (4-piece auto-classification bundle) | voice | 2026-05-10 | Superseded by F1d (user-controlled mute). Auto-classification creates unfixable social problem. |
| F1d | User-controlled mute on PC + Mobile | both | 2026-05-13 | All sub-pieces shipped. Long-press mute, voice "no sound", SMS hot-link, hosted-replies backend. |
| F2c | Walkie-talkie style turn-taking on voice | voice | 2026-05-10 | Marker-word ambiguity unresolved; latency work reduced pain. |
| F2d | Mobile auto-listen after confirmation prompts | mobile | 2026-05-23 | Irrelevant — voice phone line provides hands-free. |
| F2e | Alert state visibility + Reactivate button | both | 2026-05-23 | Both halves live in V57.21.0 build 195. `manage-rules` reactivate op + `app/alerts.tsx` UI. |
| F2f | Multi-entry alert discouragement (UI warning + Naavi prompt steering) | both | 2026-05-23 | Shipped V57.21.0 build 195. Default `one_shot=true` + Reactivate path. |
| F2g | Per-user alert channel preferences (opt out of SMS/WhatsApp/Email/Push/Voice Call) | both | 2026-05-23 | Phase 1 (server) + Phase 2 (mobile UI) shipped. Phase 3 (voice readback) deferred. |
| F2h | Contacts adapter — match by postal-address fields | server | 2026-05-28 | `addresses` added to personFields + readMask. Live-verified by Wael. |
| F3a | Picovoice Eagle voice biometric (caller voiceprint ID) | voice | 2026-05-13 | Dropped — Picovoice approval stalled. Replaced by 4-digit PIN (shipped V57.15.5 build 176). |
| F4a | Voice action parity — DELETE_EVENT, LIST_RULES, DELETE_MEMORY, SCHEDULE_MEDICATION | voice | 2026-05-19 | All three tools verified in `naavi-voice-server/src/anthropic_tools.js`. |
| F4b | Inbound SMS / WhatsApp queryability | backend | 2026-05-19 | Same conclusion as F1b. |
| F4c | Help / Discover section + Aura narration | website | 2026-05-19 | All 8 recipe pages + FAQ + Cora→Andromeda swap shipped. |
| F4d | AWS Polly voice unification — mobile → phone | voice | 2026-05-19 | Superseded by voice role split 2026-05-17 (Hera in-app / Andromeda website). |
| F4e | Public demo line Polly Joanna → Cora migration | voice | 2026-05-18 | Reverted same day — 8.6s Deepgram fetch latency + phone-layer voice deviation. |
| F5c | Executable tasks on alert fire — auto-send messages/calls when alert triggers | both | 2026-06-15 | Confirmed fully shipped and tested by Wael. Location alerts: contacts pre-resolved at set-time via `resolveTaskActions()`, stored in `action_config.task_actions[]`. Time/other alerts: Claude emits structured task objects into `tasks[]`; `evaluate-rules` resolves contact at fire-time via `lookup-contact` and executes SMS/email. Both paths confirmed working. Original audit entry was stale. |
| F5a | Picker robustness on voice (multi-option scenarios) | voice | 2026-06-12 | Tested by Wael — picker listed nearby locations, caller picked one, alert created correctly with address and coordinates. Did not reproduce. |
| F6a | Mini AI-triage support system | server | 2026-06-12 | Fully shipped: ticket system, staff portal (staff.mynaavi.com), OTP login, Claude-drafted replies, interleaved thread view, role system (superadmin/admin/staff), staffer management UI with add/deactivate/promote/demote. Auto-tester coverage exists in tests/catalogue/tickets.ts (6 tests). |
| F5c | Email instant-search live-overlay | server | 2026-06-10 | Already shipped — `fetchLiveRecentEmails` confirmed in `naavi-chat/index.ts`. |
| F5d | Outbound call to third party — MAKE_CALL action | Both | 2026-06-14 | `outbound-call` Edge Function deployed. MAKE_CALL pre-Claude bypass in `naavi-chat`. Prompt rules added. Tested by Wael — "passed perfect". Mobile confirm card (AAB) deferred as follow-up. |

---

## Closed Tooling (T)

| ID | Description | Surface | Closed | Reason |
|----|-------------|---------|--------|--------|
| T1a | Migrate both surfaces to Anthropic Structured Outputs | both | 2026-05-28 | Migration already complete — both surfaces on `tools: NAAVI_TOOLS` with typed JSON schemas. |
| T3a | Pre-invite human-tester runbook + sharp test boundary | docs | 2026-05-19 | `docs/PRE_INVITE_SMOKE_TEST.md` + `docs/TEST_RESPONSIBILITY_MATRIX.md` shipped. Automation gaps absorbed into T2a/T3c. |
| T3b | OAuth health monitoring cron | server | 2026-05-19 | Merged into B3g (now closed). |
| T2b | Phase 2 demo data — Gmail + Calendar seeding for mynaavidemo | backend | 2026-06-12 | `scripts/seed-demo-google-data.js` run successfully — 5 calendar events + 5 Gmail messages seeded for demo user. Both idempotent (re-run safe). |
| T4b | Mobile vs Voice parity audit | docs | 2026-06-12 | Audit produced: `docs/MOBILE_VS_VOICE_PARITY_AUDIT_2026-06-12.md`. 5 voice gaps + 1 mobile gap identified and ranked. Doc is now a live standing document — see CLAUDE.md rule for update obligation. |
| T4c | Soft-tick presence audit on voice | voice | 2026-06-12 | Audited all silent gaps. One real gap fixed: tick now plays on all inbound calls (including morning-brief) from connect to first user word. Bypass paths inside askClaude() were already covered (tick starts before askClaude() is called). |
| T2a | Maestro full-suite mobile UI test coverage | mobile | 2026-06-17 | 16 scenarios, 12/16 pass on emulator. 4 failures (08, 11, 12, 13) confirmed working on real phone — emulator timing/state issues only, not product bugs. Suite accepted as-is. |
| T3c | Voice regression suite — automated call-path testing for naavi-voice-server | voice | 2026-06-19 | Approach #3 shipped: `POST /test/ask` endpoint added to voice server (guarded by VOICE_TEST_SECRET), 6 tests in `tests/catalogue/voice-regression.ts` registered in runner. Covers: endpoint reachability, calendar query, contact lookup, email alert intent, location alert "arrive home" regression guard, graceful unknown intent. All 6 green. Coverage gaps: Deepgram STT accuracy, Twilio TwiML generation, real outbound action execution, audio/TTS output. |
| F2j | Production Supabase migration-tracking audit — checked every "unapplied" migration individually | server | 2026-07-02 | Found while diagnosing the F2b production rollout. Walked all 7 flagged migrations (`20260321`, `20260322`, `20260419000099`, `20260430`, `20260615`, `20260615000001`, `20260621`) against production's actual schema. **6 of 7 were pure CLI tracking false-positives** — `calendar_events`, `contacts`, `gmail_messages`, `knowledge_fragments`, `naavi_notes`, `push_subscriptions`, `reminders`, `documents`, `geofence_events`, `match_knowledge_for_dedup()`, `user_tokens` all already existed on production (created manually before formal migration tracking, same pattern already documented for staging — see `project_staging_migration_tracking_quirk`). **1 genuine, minor gap**: `user_settings` was missing `email`/`push_token`/`brief_opt_in`. Checked codebase usage before fixing — `push_token` and `brief_opt_in` are referenced nowhere in the app (dead columns, zero impact); `email` has a working fallback to `auth.users.email` already in active use (no observed symptom). Applied the missing columns to production (`20260430_user_settings_core_columns.sql`, safe `ADD COLUMN IF NOT EXISTS`). Nothing was silently broken in production from this — confirmed, not assumed. |

## Closed Architecture (ARCH)

| ID | Description | Surface | Closed | Reason |
|----|-------------|---------|--------|--------|
| ARCH-1 | Deterministic-first architecture — Layer 2 intent gate + voice regression suite | backend | 2026-06-14 | All three layers shipped. Multi-action "." separator bug fixed (normalizeActionSeparators updated for lowercase voice dictation). Path B systemic fix applied (wrapper only fires on genuine Claude uncertainty). T3c split into standalone item. Tested: Email Bob + Text Sarah in one period-separated message — both alerts created. |

---

## Closed without entry

Items walked but not added to any table. Reopen if symptom recurs.

| Holding-list # | Item | Status | Reason closed |
|---|---|---|---|
| 4 | Geofence reliability (pending phone reboot) | deleted | Tested 2026-05-08 — no problems found. |
| 12 | `naavi-spend-summary` Edge Function | completed | Function exists; Maestro test PASSED 2026-05-08. Holding-list entry was stale. |
| 14 | Demo line "remind me" time-extraction loop | deleted | Impossible by architecture — demo line is fully canned. |
| B1a | Voice live-calendar fetch (voice still on stale snapshot) | deleted | Validated 2026-05-08 — does not reproduce in real use. |
| B1b | LIST_RULES backstop on mobile | completed | V57.14.0 build 166 commit `bd52106`. Mobile orchestrator correctly lists alerts. |
| B1c | Naavi misses brand-new emails for up to an hour | completed | Live-overlay + Railway env vars fix 2026-05-09. Both surfaces verified. |
| B1d | Pre-search "Nothing matched" gag override | completed | Server side `b667115` + mobile soft-gag via subsequent AABs. |
| B3b | Cosmetic ruler leak on long-wrap user bubbles | completed (not retested) | Build 166 `opacity: 0` fix. Cosmetic only — reopen if visibly annoying. |

---

## Session notes

- Walked all 26 holding-list items one at a time with explicit user "done" signal between items (2026-05-08).
- Surface column added 2026-05-08 as cross-surface drift discipline. See CLAUDE.md Rule 16 + Voice Completion Roadmap W2/W3 for the full story.
- Major bulk closure 2026-05-19: 17 items moved to closed.
- Holding list confirmed as the single master inventory for all bugs and features 2026-06-11.
